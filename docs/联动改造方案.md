# 进销存系统联动改造方案

> 版本：v2.4 | 日期：2026-04-28 | 状态：审查修正版

---

## 一、现状诊断

### 1.1 六大断裂点

| # | 断裂 | 现状 | 根因 | 影响 |
|---|------|------|------|------|
| 1 | 项目成本→库存 | 添加材料类成本不扣库存 | ProjectCost 无 product_id/quantity | 库存数据不准 |
| 2 | 销售单→项目收入 | 完全独立，销售不生成收入 | SaleOrder 只有 project_name 文本 | 需手动录收入，易遗漏 |
| 3 | 销售单→库存 | ⚠️ 已联动但将移除 | `orders.py:180` 扣减，与领料双扣 | 改造后移除 |
| 4 | 删除成本→库存 | 不联动 | 同#1 | 库存永远少了 |
| 5 | 删除销售→项目收入 | 不联动 | 同#2 | 收入虚增 |
| 6 | 项目利润 | 成本/收入各算各的 | 两套并行机制 | 利润数据失真 |

### 1.2 核心问题：两套项目机制并存

```
机制A（订单聚合看板）         机制B（项目管理）
┌─────────────────────┐     ┌─────────────────────┐
│ SaleOrder.project_name│    │ Project.id          │
│ PurchaseOrder.project_name│ │ → ProjectCost       │
│ 文本匹配，无FK关联       │    │ → ProjectIncome     │
│ 按 project_name 聚合    │    │ 按 project_id FK关联 │
└─────────────────────┘     └─────────────────────┘
        互不相通！
```

### 1.3 架构缺陷

1. **联动逻辑散落路由层**：库存扣减/回补在 `project_costs.py` 写3遍，收入联动在 `orders.py` 写3遍，不可复用不可测试
2. **利润增量累加与重算并存**：多处手动 `project.total_cost += amount` 与 `update_project_summary` 重算冲突
3. **聚合接口依赖文本匹配**：`get_projects()` 按 `project_name` 聚合，与机制B的 `project_id` 查询完全割裂
4. **旧数据无法享受联动**：已有 SaleOrder 仅有 `project_name`，永远 `project_id = NULL`

---

## 二、改造目标（欢乐田园完整链路）

```
1. 采购单PO-001 → 12摄像头+5支架入库
   库存: 摄像头+12, 支架+5

2. 项目领料 → 12摄像头+12支架+3交换机出库
   库存: 摄像头-12, 支架-12, 交换机-3
   项目成本: +材料成本

3. 项目人工 → 向建工资400
   项目成本: +人工400
   库存: 无变化（非材料类）

4. 销售单（关联项目）→ 自动生成项目收入
   库存: ★ 不再扣减（已通过领料扣过，销售单仅记录客户和金额）

5. 客户付款 → 项目收入确认收款

6. 项目利润 = 销售收入 - (材料成本 + 人工成本 + 采购成本)
```

> **库存扣减链路决定**：改造后，库存出库统一走"项目领料"（ProjectCost 材料类联动），
> 销售单**不再扣减库存**。原因：工程项目的货物在领料时已出库，销售单只是客户结算凭证，
> 如果销售单再扣一次就会双扣。改造时需将 `crud/orders.py` 中
> `create_sale_order` 的库存扣减逻辑移除。

### 设计原则

1. **project_id FK 替代 project_name 文本** — 精确关联
2. **材料类成本联动库存** — 创建扣库存、删除回补
3. **销售单自动生成项目收入** — 创建生成、删除联动删除
4. **项目利润统一计算** — ProjectCost + PurchaseOrder 统一归集
5. **联动逻辑下沉 crud/linkage.py** — 路由层只调函数，不写业务逻辑
6. **向后兼容** — project_name 保留不删，project_id 新增可选字段
7. **旧数据一次性回填** — 迁移时自动关联，不让旧数据成孤岛

### 三大不变量（方案必须保证）

| # | 不变量 | 含义 | 违反后果 |
|---|--------|------|---------|
| I | **库存不变量** | 材料成本创建/删除/更新后，库存变化 = 数量差值；任何失败必须回滚到原值 | 库存数据不准，负库存 |
| II | **收入不变量** | 同一 SaleOrder 最多对应一条 `ProjectIncome(source_type='sale_order', source_id=so.id)` | 收入虚增/重复 |
| III | **汇总不变量** | `projects.total_income/total_cost/profit` 永远可由明细重算得到（不依赖增量） | 利润数据失真 |

---

## 三、步骤1：数据层（模型 + 迁移 + 旧数据回填）

### models.py 新增字段

```python
# ── SaleOrder 新增 ──
project_id = Column(Integer, ForeignKey("projects.id"), nullable=True, index=True,
                    comment="关联项目ID")

# ── PurchaseOrder 新增 ──
project_id = Column(Integer, ForeignKey("projects.id"), nullable=True, index=True,
                    comment="关联项目ID")

# ── ProjectCost 新增 ──
product_id = Column(Integer, ForeignKey("products.id"), nullable=True,
                    comment="商品ID（材料类成本关联库存）")
quantity = Column(Integer, nullable=True,
                  comment="数量（材料类成本关联库存）")

# ── ProjectIncome 新增 ──
source_type = Column(String(20), nullable=True, default="manual",
                     comment="来源: manual=手动 / sale_order=销售单自动生成")
source_id = Column(Integer, nullable=True,
                   comment="来源ID（sale_order时为销售单ID）")

# ── ProjectIncome 新增 UNIQUE 约束（保证收入不变量 II）──
# 同一 source_type + source_id 最多一条记录，防止重复生成
# ★ SQLite 旧表：ALTER TABLE 不支持 ADD CONSTRAINT，靠迁移中 CREATE UNIQUE INDEX 生效
# ★ 新库/非SQLite：__table_args__ 的 UniqueConstraint 在 CREATE TABLE 时生效
__table_args__ = (
    UniqueConstraint("source_type", "source_id", name="uq_income_source"),
    # ... 保留已有的其他 table_args ...
)
```

> ProjectCost 表没有 account_id 字段，联动库存时通过路由注入的 account_id 间接获取。
> UNIQUE 约束 `uq_income_source` 保证收入不变量 II：同一销售单不会生成重复的项目收入。手动录入的 source_type='manual' + source_id=NULL 不受约束影响（SQLite 将 NULL 视为不同值）。
> **SQLite 注意**：`ALTER TABLE ADD CONSTRAINT` 不被 SQLite 支持，旧表靠迁移中 `CREATE UNIQUE INDEX uq_income_source` 生效；`__table_args__` 中的 `UniqueConstraint` 主要给新库/非SQLite场景，旧库迁移不依赖它。

### database.py 迁移函数（含索引 + 旧数据回填）

```python
def _migrate_linkage(engine):
    """联动改造：新增字段 + 索引 + 旧数据回填"""
    insp = inspect(engine)

    # ── 1. sale_orders 增加 project_id + 索引 ──
    if "sale_orders" in insp.get_table_names():
        cols = [c["name"] for c in insp.get_columns("sale_orders")]
        if "project_id" not in cols:
            with engine.connect() as conn:
                conn.execute(text("ALTER TABLE sale_orders ADD COLUMN project_id INTEGER REFERENCES projects(id)"))
                conn.execute(text("CREATE INDEX IF NOT EXISTS ix_sale_orders_project_id ON sale_orders(project_id)"))
                conn.commit()
            logger.info("迁移: sale_orders 表添加 project_id 列及索引")

    # ── 2. purchase_orders 增加 project_id + 索引 ──
    if "purchase_orders" in insp.get_table_names():
        cols = [c["name"] for c in insp.get_columns("purchase_orders")]
        if "project_id" not in cols:
            with engine.connect() as conn:
                conn.execute(text("ALTER TABLE purchase_orders ADD COLUMN project_id INTEGER REFERENCES projects(id)"))
                conn.execute(text("CREATE INDEX IF NOT EXISTS ix_purchase_orders_project_id ON purchase_orders(project_id)"))
                conn.commit()
            logger.info("迁移: purchase_orders 表添加 project_id 列及索引")

    # ── 3. project_costs 增加 product_id, quantity ──
    if "project_costs" in insp.get_table_names():
        cols = [c["name"] for c in insp.get_columns("project_costs")]
        if "product_id" not in cols:
            with engine.connect() as conn:
                conn.execute(text("ALTER TABLE project_costs ADD COLUMN product_id INTEGER REFERENCES products(id)"))
                conn.commit()
            logger.info("迁移: project_costs 表添加 product_id 列")
        if "quantity" not in cols:
            with engine.connect() as conn:
                conn.execute(text("ALTER TABLE project_costs ADD COLUMN quantity INTEGER"))
                conn.commit()
            logger.info("迁移: project_costs 表添加 quantity 列")

    # ── 4. project_incomes 增加 source_type, source_id + UNIQUE 索引 ──
    if "project_incomes" in insp.get_table_names():
        cols = [c["name"] for c in insp.get_columns("project_incomes")]
        if "source_type" not in cols:
            with engine.connect() as conn:
                conn.execute(text("ALTER TABLE project_incomes ADD COLUMN source_type VARCHAR(20) DEFAULT 'manual'"))
                conn.commit()
        if "source_id" not in cols:
            with engine.connect() as conn:
                conn.execute(text("ALTER TABLE project_incomes ADD COLUMN source_id INTEGER"))
                conn.commit()
        # ★ 保证收入不变量 II：同一 source_type + source_id 唯一
        # SQLite 不支持 ALTER TABLE ADD CONSTRAINT，用 CREATE UNIQUE INDEX 替代
        # 注意：如果已有重复数据，建索引会失败，需先去重
        with engine.connect() as conn:
            # 先去重：保留最早的，删除重复的
            conn.execute(text("""
                DELETE FROM project_incomes WHERE rowid NOT IN (
                    SELECT MIN(rowid) FROM project_incomes
                    GROUP BY source_type, source_id
                )
            """))
            conn.commit()
            conn.execute(text(
                "CREATE UNIQUE INDEX IF NOT EXISTS uq_income_source "
                "ON project_incomes(source_type, source_id)"
            ))
            conn.commit()
            logger.info("迁移: project_incomes 表添加 uq_income_source 唯一索引")

    # ★ source_id 单独索引：sale_delete_income 按 source_type+source_id 查询
    # uq_income_source 已覆盖 (source_type, source_id) 组合查询，但 source_id 单列
    # 也需要索引以支持未来可能的按 source_id 查询场景
    if "project_incomes" in insp.get_table_names():
        with engine.connect() as conn:
            conn.execute(text(
                "CREATE INDEX IF NOT EXISTS ix_project_incomes_source_id "
                "ON project_incomes(source_id)"
            ))
            conn.commit()

    # ── 5. 强制项目名唯一 + 旧数据回填：project_name → project_id ──
    # ★ 同名项目是数据错误，绝不允许"随机绑最早"——那会把历史单据绑错项目
    # 策略：同名项目先重命名（加后缀），再建UNIQUE约束，再回填

    # 5a. 检测并消除同名项目
    with engine.connect() as conn:
        duplicates = conn.execute(text("""
            SELECT account_id, name, COUNT(*) as cnt
            FROM projects
            GROUP BY account_id, name
            HAVING cnt > 1
        """)).fetchall()
        if duplicates:
            logger.warning(f"回填: 发现 {len(duplicates)} 组同名项目，自动加后缀消除")
            # 对同名项目，保留最早创建的，其余加后缀 _2, _3, ...
            for account_id_val, dup_name, cnt in duplicates:
                rows = conn.execute(text("""
                    SELECT id FROM projects
                    WHERE account_id = :aid AND name = :n
                    ORDER BY created_at ASC
                """), {"aid": account_id_val, "n": dup_name}).fetchall()
                for i, row in enumerate(rows):
                    if i > 0:
                        new_name = f"{dup_name}_{i + 1}"
                        conn.execute(text(
                            "UPDATE projects SET name = :nn WHERE id = :pid"
                        ), {"nn": new_name, "pid": row[0]})
                        logger.info(f"回填: 项目ID={row[0]} '{dup_name}' 重命名为 '{new_name}'")
            conn.commit()

    # 5b. 建UNIQUE约束：同账号下项目名唯一（防止未来再出同名）
    with engine.connect() as conn:
        conn.execute(text(
            "CREATE UNIQUE INDEX IF NOT EXISTS uq_projects_account_name "
            "ON projects(account_id, name)"
        ))
        conn.commit()
        logger.info("迁移: projects 表添加 uq_projects_account_name 唯一索引")

    # 5c. 安全回填：现在项目名已唯一，project_name → project_id 精确匹配
    with engine.connect() as conn:
        conn.execute(text("""
            UPDATE sale_orders
            SET project_id = (
                SELECT p.id FROM projects p
                WHERE p.name = sale_orders.project_name
                  AND p.account_id = sale_orders.account_id
                LIMIT 1
            )
            WHERE project_name IS NOT NULL AND project_name != '' AND project_id IS NULL
        """))
        conn.execute(text("""
            UPDATE purchase_orders
            SET project_id = (
                SELECT p.id FROM projects p
                WHERE p.name = purchase_orders.project_name
                  AND p.account_id = purchase_orders.account_id
                LIMIT 1
            )
            WHERE project_name IS NOT NULL AND project_name != '' AND project_id IS NULL
        """))
        conn.commit()

    # ── 6. 回填销售单收入：为已关联项目的旧销售单自动生成 ProjectIncome ──
    with engine.connect() as conn:
        conn.execute(text("""
            INSERT INTO project_incomes (project_id, amount, payment_status, invoice_status, notes, source_type, source_id, income_date)
            SELECT so.project_id, so.total_price, 'pending',
                   CASE WHEN so.has_invoice THEN '已开' ELSE '未开' END,
                   '数据迁移：销售单 ' || so.order_no || ' 回填生成',
                   'sale_order', so.id, so.sale_date
            FROM sale_orders so
            WHERE so.project_id IS NOT NULL
              AND NOT EXISTS (
                  SELECT 1 FROM project_incomes pi
                  WHERE pi.source_type = 'sale_order' AND pi.source_id = so.id
              )
        """))
        conn.commit()
    logger.info("迁移: 旧数据 project_id 回填 + 销售单收入回填完成")


def init_db():
    import models
    Base.metadata.create_all(bind=engine)
    _migrate_image_url(engine)
    _migrate_operator(engine)
    _migrate_linkage(engine)  # 新增
```

> SQLite 的 ALTER TABLE ADD COLUMN 不会自动创建索引，必须显式 CREATE INDEX。
> 回填基于 `project_name = Project.name` 精确匹配，未匹配的保留 `project_id = NULL` 不受影响。
> **同名项目策略**：同名项目是数据错误，不允许"随机绑最早"。迁移时先自动重命名同名项目（加后缀），再建 `UNIQUE(account_id, name)` 约束彻底杜绝同名，最后才安全回填。

### schemas.py 改动

```python
# SaleOrderCreate / SaleOrderOut 新增
project_id: Optional[int] = None

# PurchaseOrderCreate / PurchaseOrderOut 新增
project_id: Optional[int] = None

# ProjectCostCreate 新增
product_id: Optional[int] = None
quantity: Optional[int] = None
unit_price: Optional[float] = None  # 单价（仅前端辅助计算用，后端不存储）

# ProjectCostUpdate 新增
product_id: Optional[int] = None
quantity: Optional[int] = None

# ProjectCostOut 新增
product_id: Optional[int] = None
quantity: Optional[int] = None
product_name: Optional[str] = None  # 展示用

# ProjectIncomeCreate 新增
source_type: Optional[str] = None
source_id: Optional[int] = None

# ProjectIncomeOut 新增
source_type: Optional[str] = None
source_id: Optional[int] = None
```

---

## 四、步骤2：联动业务层（crud/linkage.py）

将联动逻辑从路由层抽取为独立函数，路由层只负责调用：

```python
# crud/linkage.py — 联动业务逻辑层

"""库存↔成本、销售单↔项目收入 的联动操作，独立于路由层

三大不变量保证：
  I.  库存不变量：材料成本变动后库存变化 = 数量差值，失败自动回滚
  II. 收入不变量：同一 SaleOrder 最多一条 source_type='sale_order' 的 ProjectIncome
  III.汇总不变量：所有联动完成后调用 update_project_summary 重算，不依赖增量
"""

from sqlalchemy.orm import Session
from typing import Optional
import models
from .base import _log, get_or_create_inventory
from fastapi import HTTPException


# ── 成本 ↔ 库存（保证库存不变量 I）──

def cost_deduct_inventory(db: Session, account_id: int, cost: models.ProjectCost, operator: str = "user"):
    """材料类成本扣减库存（创建时调用）

    不变量 I 保证：扣减前校验库存充足，扣减后 inv.quantity >= 0。
    失败时抛出 HTTPException，由路由层 rollback 恢复原值。
    """
    if cost.cost_type != "材料" or not cost.product_id or not cost.quantity:
        return
    product = db.query(models.Product).filter(
        models.Product.id == cost.product_id,
        models.Product.account_id == account_id
    ).first()
    if not product:
        raise HTTPException(status_code=404, detail=f"商品不存在: ID={cost.product_id}")
    inv = get_or_create_inventory(db, account_id, cost.product_id)
    if inv.quantity < cost.quantity:
        raise HTTPException(status_code=400,
            detail=f"库存不足: {product.name} 当前库存{inv.quantity}, 需要出库{cost.quantity}")
    inv.quantity -= cost.quantity
    assert inv.quantity >= 0, f"库存不变量违反: {product.name} 库存变为 {inv.quantity}"
    _log(db, account_id, "adjust", "inventory", inv.id,
         f"项目领料扣库存: {product.name} -{cost.quantity}", operator=operator)


def cost_restore_inventory(db: Session, account_id: int, cost: models.ProjectCost, operator: str = "user"):
    """材料类成本回补库存（删除时调用）

    不变量 I 保证：回补操作只会增加库存，不可能违反不变量。
    """
    if cost.cost_type != "材料" or not cost.product_id or not cost.quantity:
        return
    inv = get_or_create_inventory(db, account_id, cost.product_id)
    inv.quantity += cost.quantity
    _log(db, account_id, "adjust", "inventory", inv.id,
         f"删除项目成本回补库存: +{cost.quantity}", operator=operator)


def cost_update_inventory(db: Session, account_id: int, cost: models.ProjectCost,
                          old_cost_type: Optional[str], old_product_id: Optional[int],
                          old_quantity: Optional[int],
                          operator: str = "user"):
    """更新成本时调整库存差值（先回补旧的，再扣减新的）

    不变量 I 保证：
    1. 先回补旧库存（安全操作，只会增加）
    2. 再扣减新库存（含库存不足校验 + assert）
    3. 如果扣减新库存失败（HTTPException），路由层 rollback 会同时撤销回补，
       库存恢复到操作前的原值

    空值防护：旧值参数可为 None（旧记录没有 product_id/quantity），
    此时旧记录未关联库存，无需回补。
    """
    # 回补旧库存（空值防护：旧记录可能没有 product_id/quantity）
    old_quantity = old_quantity or 0
    if old_cost_type == "材料" and old_product_id and old_quantity > 0:
        inv = get_or_create_inventory(db, account_id, old_product_id)
        inv.quantity += old_quantity
        _log(db, account_id, "adjust", "inventory", inv.id,
             f"更新成本回补旧库存: +{old_quantity}", operator=operator)
    # 扣减新库存（含库存不足校验 + 不变量断言）
    cost_deduct_inventory(db, account_id, cost, operator)


# ── 销售单 ↔ 项目收入（保证收入不变量 II）──

def sale_create_income(db: Session, account_id: int, order: models.SaleOrder, operator: str = "user"):
    """销售单关联项目时自动生成项目收入

    不变量 II 保证：
    1. 幂等检查：如果已存在同 source_type+source_id 的收入，跳过不重复创建
    2. 数据库层 UNIQUE 索引 uq_income_source 作为最终防线
    """
    if not order.project_id:
        return
    # ★ 幂等检查：保证不变量 II
    existing = db.query(models.ProjectIncome).filter(
        models.ProjectIncome.source_type == "sale_order",
        models.ProjectIncome.source_id == order.id
    ).first()
    if existing:
        return  # 已存在，幂等跳过
    project = db.query(models.Project).filter(
        models.Project.id == order.project_id,
        models.Project.account_id == account_id
    ).first()
    if not project:
        return
    income = models.ProjectIncome(
        project_id=order.project_id,
        amount=order.total_price,
        payment_status="pending",
        invoice_status="未开" if not order.has_invoice else "已开",
        notes=f"销售单 {order.order_no} 自动生成",
        source_type="sale_order",
        source_id=order.id
    )
    db.add(income)
    db.flush()
    _log(db, account_id, "create", "project_income", income.id,
         f"销售单 {order.order_no} 自动生成项目收入: ¥{order.total_price}", operator=operator)


def sale_delete_income(db: Session, account_id: int, order_id: int, operator: str = "user"):
    """删除/取消销售单时联动删除项目收入，返回受影响的 project_id

    不变量 II 保证：按 source_type+source_id 精确查找并删除，
    UNIQUE 约束保证最多一条，不会误删或漏删。
    """
    linked_income = db.query(models.ProjectIncome).filter(
        models.ProjectIncome.source_type == "sale_order",
        models.ProjectIncome.source_id == order_id
    ).first()
    if linked_income:
        _log(db, account_id, "delete", "project_income", linked_income.id,
             f"销售单联动删除项目收入", operator=operator)
        db.delete(linked_income)
        return linked_income.project_id
    return None
```

---

## 五、步骤3：统一利润计算（utils.py）

改造 `update_project_summary`，消除所有手动增量累加：

```python
from sqlalchemy import func as sqlfunc
from models import Project, ProjectCost, ProjectIncome, PurchaseOrder


def update_project_summary(db: Session, project_id: int):
    """更新项目汇总数据（统一利润计算：项目成本 + 采购成本）

    所有联动场景统一调用此函数，不再在各路由中手动增量累加。

    ★ 此函数只计算+赋值，不调用 db.commit()。
    由路由/服务层统一 commit，保证与联动操作在同一事务中原子生效。
    """
    project = db.query(Project).filter(Project.id == project_id).first()
    if not project:
        return

    # 1. 项目收入 = ProjectIncome 合计
    total_income = db.query(sqlfunc.coalesce(sqlfunc.sum(ProjectIncome.amount), 0)).filter(
        ProjectIncome.project_id == project_id
    ).scalar()

    # 2. 项目成本 = ProjectCost 合计
    project_cost = db.query(sqlfunc.coalesce(sqlfunc.sum(ProjectCost.amount), 0)).filter(
        ProjectCost.project_id == project_id
    ).scalar()

    # 3. 采购成本 = 关联该项目的已完成采购单合计
    purchase_cost = db.query(sqlfunc.coalesce(sqlfunc.sum(PurchaseOrder.total_price), 0)).filter(
        PurchaseOrder.project_id == project_id,
        PurchaseOrder.status == "completed"
    ).scalar()

    # 4. 统一利润
    total_cost = project_cost + purchase_cost
    profit = total_income - total_cost

    project.total_income = round(total_income, 2)
    project.total_cost = round(total_cost, 2)
    project.profit = round(profit, 2)
    # ★ 不 commit，由调用方统一 commit
```

> **设计决定**：`update_project_summary` 不内含 `db.commit()`，原因是：
> 1. **事务编排权归调用方**：路由层需要"联动+汇总"在同一个 commit 中原子生效，若函数内部 commit 会破坏事务边界
> 2. **测试友好**：测试中可在调用后 assert 赋值结果，再决定 commit 或 rollback
> 3. **reconcile 对账场景**：需要批量更新多个项目后统一 commit，而非每个项目各 commit 一次

### 必须删除的手动增量累加

| 文件 | 删除的代码 | 替换为 |
|------|-----------|--------|
| `routers/project_costs.py` add | `project.total_cost += cost.amount; project.profit = ...` | `update_project_summary(db, cost.project_id)` |
| `routers/project_costs.py` delete | `project.total_cost -= cost.amount; project.profit = ...` | `update_project_summary(db, cost.project_id)` |
| `routers/project_costs.py` update | `project.total_cost = ... - old + new` | `update_project_summary(db, cost.project_id)` |
| `routers/project_costs.py` add_income | `project.total_income += income.amount; project.profit = ...` | `update_project_summary(db, income.project_id)` |
| `routers/project_costs.py` delete_income | `project.total_income -= income.amount; project.profit = ...` | `update_project_summary(db, income.project_id)` |
| `routers/project_costs.py` update_income | `project.total_income = ... - old + new` | `update_project_summary(db, income.project_id)` |

---

## 六、步骤3.5：不变量验证 + 对账（utils.py + routers/projects.py）

### 不变量验证函数

```python
# utils.py — 不变量验证

def verify_invariants(db: Session, account_id: int = None) -> dict:
    """验证三大不变量，返回违规项列表

    可在测试中调用，也可通过 /api/projects/verify-invariants API 触发。
    """
    violations = []

    # ── 不变量 I：库存不变量 ──
    # 硬校验：所有 inventory.quantity >= 0
    from models import Inventory
    query = db.query(Inventory)
    if account_id:
        query = query.filter(Inventory.account_id == account_id)
    negative_inv = query.filter(Inventory.quantity < 0).all()
    for inv in negative_inv:
        violations.append({
            "invariant": "I",
            "type": "negative_inventory",
            "detail": f"商品ID={inv.product_id} 库存={inv.quantity} < 0"
        })
    # 注：完整的库存一致性校验（采购入库 - 销售出库 - 项目领料 = 当前库存）
    # 需要遍历所有库存变动记录做交叉对比，计算量大，留作后续增强

    # ── 不变量 II：收入不变量 ──
    # 校验：同一 source_type + source_id 最多一条记录
    from models import ProjectIncome
    duplicates = db.query(
        ProjectIncome.source_type, ProjectIncome.source_id,
        sqlfunc.count(ProjectIncome.id).label("cnt")
    ).filter(
        ProjectIncome.source_type.isnot(None),
        ProjectIncome.source_id.isnot(None)
    ).group_by(
        ProjectIncome.source_type, ProjectIncome.source_id
    ).having(sqlfunc.count(ProjectIncome.id) > 1).all()
    for source_type, source_id, cnt in duplicates:
        violations.append({
            "invariant": "II",
            "type": "duplicate_income",
            "detail": f"source_type={source_type}, source_id={source_id} 存在{cnt}条记录（应最多1条）"
        })

    # ── 不变量 III：汇总不变量 ──
    # 校验：projects.total_income/total_cost/profit 与明细重算一致
    from models import Project, PurchaseOrder
    projects_query = db.query(Project)
    if account_id:
        projects_query = projects_query.filter(Project.account_id == account_id)
    for project in projects_query.all():
        calc_income = db.query(sqlfunc.coalesce(sqlfunc.sum(ProjectIncome.amount), 0)).filter(
            ProjectIncome.project_id == project.id
        ).scalar()
        calc_cost = db.query(sqlfunc.coalesce(sqlfunc.sum(ProjectCost.amount), 0)).filter(
            ProjectCost.project_id == project.id
        ).scalar()
        calc_purchase = db.query(sqlfunc.coalesce(sqlfunc.sum(PurchaseOrder.total_price), 0)).filter(
            PurchaseOrder.project_id == project.id,
            PurchaseOrder.status == "completed"
        ).scalar()
        calc_total_cost = calc_cost + calc_purchase
        calc_profit = calc_income - calc_total_cost

        if abs(project.total_income - round(calc_income, 2)) > 0.01:
            violations.append({
                "invariant": "III",
                "type": "income_mismatch",
                "detail": f"项目ID={project.id} stored_total_income={project.total_income} != calc={round(calc_income, 2)}"
            })
        if abs(project.total_cost - round(calc_total_cost, 2)) > 0.01:
            violations.append({
                "invariant": "III",
                "type": "cost_mismatch",
                "detail": f"项目ID={project.id} stored_total_cost={project.total_cost} != calc={round(calc_total_cost, 2)}"
            })
        if abs(project.profit - round(calc_profit, 2)) > 0.01:
            violations.append({
                "invariant": "III",
                "type": "profit_mismatch",
                "detail": f"项目ID={project.id} stored_profit={project.profit} != calc={round(calc_profit, 2)}"
            })

    return {
        "ok": len(violations) == 0,
        "violation_count": len(violations),
        "violations": violations
    }
```

### 对账 API

```python
# routers/projects.py — 新增对账端点

@router.post("/verify-invariants")
async def verify_invariants_endpoint(
    db: Session = Depends(get_db),
    account_id: int = Depends(get_account_id)
):
    """验证三大不变量（库存/收入/汇总），返回违规项"""
    from utils import verify_invariants
    return verify_invariants(db, account_id)


@router.post("/reconcile")
async def reconcile_endpoint(
    db: Session = Depends(get_db),
    account_id: int = Depends(get_account_id)
):
    """对账：重算所有项目汇总，修复汇总不变量 III 违规"""
    from utils import update_project_summary
    from models import Project
    projects = db.query(Project).filter(Project.account_id == account_id).all()
    fixed = []
    for project in projects:
        old_income = project.total_income
        old_cost = project.total_cost
        old_profit = project.profit
        update_project_summary(db, project.id)  # 只计算+赋值，不commit
        if (abs(old_income - project.total_income) > 0.01 or
            abs(old_cost - project.total_cost) > 0.01 or
            abs(old_profit - project.profit) > 0.01):
            fixed.append({
                "project_id": project.id,
                "name": project.name,
                "before": {"income": old_income, "cost": old_cost, "profit": old_profit},
                "after": {"income": project.total_income, "cost": project.total_cost, "profit": project.profit}
            })
    db.commit()  # 统一一次 commit
    return {"fixed_count": len(fixed), "fixed": fixed}
```

> `verify-invariants` 用于日常巡检和测试断言；`reconcile` 用于修复历史数据不一致。

---

## 七、步骤4：路由层改造（调用 linkage + update_project_summary）

### 6.1 成本联动库存（routers/project_costs.py）

> **事务模式**：所有联动 + 汇总重算在同一个 `db.commit()` 之前完成，保证汇总不变量 III。
> **失败回滚铁律**：任何联动步骤抛出异常（库存不足、商品不存在等），必须 `db.rollback()` 撤销整个事务，
> 绝不允许出现"成本已插入但库存没扣"的脏数据。这就是为什么 `db.flush()` 而非 `db.commit()`：
> flush 让 ORM 写入数据库但未提交，rollback 可撤销；commit 后不可撤销。

**创建成本**：

```python
from crud.linkage import cost_deduct_inventory
from utils import update_project_summary

@router.post("/")
def add_project_cost(...):
    try:
        cost = ProjectCost(
            project_id=project_id,
            cost_type=cost_data.cost_type,
            amount=cost_data.amount,
            product_id=cost_data.product_id,      # 新增
            quantity=cost_data.quantity,            # 新增
            # ... 其他字段 ...
        )
        db.add(cost)
        db.flush()  # 拿到 cost.id，但不提交事务

        # ★ 联动：材料类成本扣减库存（不变量 I）
        cost_deduct_inventory(db, account_id, cost, operator)

        # ★ 统一重算项目汇总（不变量 III，在 commit 之前）
        update_project_summary(db, cost.project_id)

        db.commit()  # 一次提交，成本+库存+汇总原子生效

        return schemas.ProjectCostOut(...)
    except HTTPException:
        db.rollback()
        raise
    except Exception:
        db.rollback()
        raise
```

**删除成本**：

```python
from crud.linkage import cost_restore_inventory

@router.delete("/{cost_id}")
def delete_project_cost(...):
    cost = ...
    project_id = cost.project_id  # ★ 先保存，删除后 cost 对象失效
    try:
        # ★ 联动：材料类成本回补库存（不变量 I，在 delete 之前）
        cost_restore_inventory(db, account_id, cost, operator)
        db.delete(cost)

        # ★ 统一重算项目汇总（不变量 III）
        update_project_summary(db, project_id)

        db.commit()  # 一次提交，删除+回补+汇总原子生效
    except ...
```

**更新成本**：

```python
from crud.linkage import cost_update_inventory

@router.put("/{cost_id}")
def update_project_cost(...):
    cost = ...
    try:
        # ★ 记录旧值（允许 None：旧记录可能没有 product_id/quantity）
        old_cost_type = cost.cost_type
        old_product_id = cost.product_id   # 可为 None
        old_quantity = cost.quantity        # 可为 None

        # 执行字段更新
        update_data = cost_update.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if field == "cost_date" and value:
                try: setattr(cost, field, datetime.strptime(value, "%Y-%m-%d"))
                except: pass
            else:
                setattr(cost, field, value)

        # ★ 联动：先回补旧的，再扣减新的（不变量 I）
        cost_update_inventory(db, account_id, cost,
                              old_cost_type, old_product_id, old_quantity, operator)

        # ★ 统一重算项目汇总（不变量 III）
        update_project_summary(db, cost.project_id)

        db.commit()  # 一次提交，更新+库存调整+汇总原子生效
    except ...
```

### 6.2 销售单联动项目收入（crud/orders.py）

**创建销售单**（必须显式赋值 `project_id`）：

```python
from crud.linkage import sale_create_income
from utils import update_project_summary

def create_sale_order(db: Session, account_id: int, data: schemas.SaleOrderCreate):
    try:
        order = models.SaleOrder(
            account_id=account_id,
            order_no=order_no,
            customer_id=data.customer_id,
            project_id=data.project_id,     # ★ 显式赋值
            # ★ project_name 自动填充：前端不传，后端从 project_id 反查
            project_name=data.project_name or (
                db.query(models.Project.name).filter(
                    models.Project.id == data.project_id
                ).scalar() if data.project_id else None
            ),
            status="completed",
            notes=data.notes,
            total_price=0
        )
        db.add(order)
        db.flush()

        # ... 原有的商品处理逻辑不变 ...

        order.total_price = round(total, 2)

        # ★ 联动：自动生成项目收入（不变量 II，幂等）
        sale_create_income(db, account_id, order, operator)

        # ★ 统一重算项目汇总（不变量 III，在 commit 之前）
        if data.project_id:
            update_project_summary(db, data.project_id)

        db.commit()  # 一次提交，订单+库存+收入+汇总原子生效

        db.refresh(order)
        return order
    except Exception:
        db.rollback()
        raise
```

**删除销售单**：

```python
from crud.linkage import sale_delete_income

def delete_sale_order(db, account_id, order_id, operator):
    order = ...
    project_id_for_summary = None
    try:
        # ★ 销售单删除不再回补库存（库存出库统一走项目领料）

        # ★ 联动：删除自动生成的项目收入（不变量 II）
        project_id_for_summary = sale_delete_income(db, account_id, order_id, operator)
        # 如果 sale_delete_income 没返回 project_id，用 order.project_id
        if not project_id_for_summary and order.project_id:
            project_id_for_summary = order.project_id

        db.delete(order)

        # ★ 统一重算项目汇总（不变量 III）
        if project_id_for_summary:
            update_project_summary(db, project_id_for_summary)

        db.commit()  # 一次提交，删除+回补+汇总原子生效
    except ...
```

**更新销售单（取消/恢复）**：

```python
def update_sale_order(db, account_id, order_id, data, operator):
    order = ...
    try:
        old_status = order.status
        for k, v in data.model_dump(exclude_unset=True).items():
            setattr(order, k, v)
        new_status = order.status

        # ★ 销售单不再扣减库存（库存出库统一走项目领料）
        # 如果未来需要"非项目销售直接出库"场景，可在此加条件判断

        # ★ 联动1：取消时删除项目收入（不变量 II）
        project_id_to_update = None
        if new_status == "cancelled" and order.project_id:
            project_id_to_update = sale_delete_income(db, account_id, order_id, operator)

        # ★ 联动2：恢复时重新生成项目收入（不变量 II，幂等）
        if old_status == "cancelled" and new_status == "completed" and order.project_id:
            sale_create_income(db, account_id, order, operator)
            project_id_to_update = order.project_id

        # ★ 统一重算项目汇总（不变量 III，在 commit 之前）
        if project_id_to_update:
            update_project_summary(db, project_id_to_update)

        db.commit()  # 一次提交，更新+库存+收入+汇总原子生效
    except ...
```

### 6.3 项目删除校验 + 级联回补（routers/projects.py）

项目删除是高风险操作，需要前置校验：

```python
from crud.linkage import cost_restore_inventory

@router.delete("/{project_id}")
def delete_project(project_id: int, db: Session = Depends(get_db),
                   account_id: int = Depends(get_account_id),
                   operator: str = Depends(get_operator)):
    project = db.query(Project).filter(
        Project.id == project_id, Project.account_id == account_id
    ).first()
    if not project:
        raise HTTPException(status_code=404, detail="项目不存在")

    # ── 前置校验：存在关联的未取消销售单/采购单时禁止删除 ──
    active_sales = db.query(SaleOrder).filter(
        SaleOrder.project_id == project_id,
        SaleOrder.status != "cancelled"
    ).count()
    if active_sales > 0:
        raise HTTPException(status_code=400,
            detail=f"项目下有 {active_sales} 条未取消的销售单，请先取消或解除关联")

    active_purchases = db.query(PurchaseOrder).filter(
        PurchaseOrder.project_id == project_id,
        PurchaseOrder.status != "cancelled"
    ).count()
    if active_purchases > 0:
        raise HTTPException(status_code=400,
            detail=f"项目下有 {active_purchases} 条未取消的采购单，请先取消或解除关联")

    try:
        # ── 级联回补：材料类成本的库存必须在 cascade 删除前回补 ──
        for cost in project.costs:
            cost_restore_inventory(db, account_id, cost, operator)

        db.delete(project)  # cascade 会自动删除关联的 costs/incomes
        db.commit()
    except Exception:
        db.rollback()
        raise
```

> **统一库存联动入口**：当前只有 `ProjectCost` 影响库存，但后续可能增加"项目领料单"等独立表。
> 所有影响库存的操作都应通过 `crud/linkage.py` 的函数入口（`cost_deduct_inventory` / `cost_restore_inventory`），
> 新增库存联动表时只需在 linkage.py 中增加对应函数，路由层调用即可。
> 如果未来表增多，可进一步抽取为 `InventoryAdjuster` 类，提供统一的 `deduct`/`restore` 接口。

### 6.4 聚合接口改造（routers/projects.py）

废弃按 `project_name` 文本聚合，统一从 Project 表出发通过 `project_id` 查询关联订单：

```python
@router.get("/")
async def get_projects(db: Session = Depends(get_db),
                       account_id: int = Depends(get_account_id)):
    """获取项目列表（统一从 Project 表出发，废弃 project_name 文本聚合）

    ★ 使用 GROUP BY 聚合一次查出关联订单数，避免 N+1 查询
    """
    from sqlalchemy import func as sqlfunc

    projects = db.query(Project).filter(
        Project.account_id == account_id
    ).order_by(Project.created_at.desc()).all()

    if not projects:
        return {"items": []}

    project_ids = [p.id for p in projects]

    # ★ 批量聚合：一次查出所有项目的关联订单数
    sale_counts = dict(
        db.query(SaleOrder.project_id, sqlfunc.count(SaleOrder.id))
        .filter(SaleOrder.project_id.in_(project_ids))
        .group_by(SaleOrder.project_id).all()
    )
    purchase_counts = dict(
        db.query(PurchaseOrder.project_id, sqlfunc.count(PurchaseOrder.id))
        .filter(PurchaseOrder.project_id.in_(project_ids))
        .group_by(PurchaseOrder.project_id).all()
    )

    items = []
    for project in projects:
        items.append({
            "id": project.id,
            "name": project.name,
            "customer_name": project.customer.name if project.customer else None,
            "status": project.status,
            "total_income": project.total_income,
            "total_cost": project.total_cost,
            "profit": project.profit,
            "sale_count": sale_counts.get(project.id, 0),
            "purchase_count": purchase_counts.get(project.id, 0),
        })
    return {"items": items}
```

### 6.5 项目详情返回关联采购单

```python
# 在 get_project_detail 中增加
purchases = db.query(models.PurchaseOrder).filter(
    models.PurchaseOrder.project_id == project_id,
    models.PurchaseOrder.account_id == account_id
).order_by(models.PurchaseOrder.purchase_date.desc()).all()

# 返回结果中增加
"purchases": [
    {
        "order_no": p.order_no,
        "supplier_name": p.supplier.name if p.supplier else None,
        "total_price": p.total_price,
        "status": p.status,
        "purchase_date": p.purchase_date.isoformat() if p.purchase_date else None
    } for p in purchases
]
```

---

## 八、步骤5：前端改动

### 8.1 销售单新建弹窗（Sales.vue）

项目输入从文本下拉改为项目ID下拉：

```vue
<!-- 原来 -->
<el-select v-model="form.project_name" filterable clearable>
  <el-option v-for="p in projects" :key="p" :label="p" :value="p" />
</el-select>

<!-- 改为 -->
<el-select v-model="form.project_id" filterable clearable placeholder="选择项目(可空)">
  <el-option v-for="p in projectList" :key="p.id" :label="p.name" :value="p.id" />
</el-select>
```

数据源从机制A切换到机制B：

```javascript
// 原来：从聚合看板取 project_name 文本列表
// projects.value = projRes.items ? projRes.items.map(item => item.project_name) : []

// 改为：从项目管理取项目列表（含 id 和 name）
const [pRes, cRes, projRes] = await Promise.all([
  api.getProducts({ page: 1, page_size: 1000 }),
  api.getCustomers({ page: 1, page_size: 1000 }),
  api.getProjectList()   // ★ 改用机制B的API
])
products.value = pRes.items || pRes
customers.value = cRes.items || cRes
projectList.value = projRes.items || projRes  // {id, name} 对象列表
```

提交时传 `project_id`，**不再需要前端传 `project_name`**：

```javascript
await api.createSale({
  customer_id: form.value.customer_id,
  project_id: form.value.project_id,     // ★ 核心字段
  // project_name 不再由前端传，后端自动从 project_id 反查填充
  has_invoice: form.value.has_invoice,
  payment_status: form.value.payment_status,
  notes: form.value.notes,
  items: validItems.map(i => ({ product_id: i.product_id, quantity: i.quantity, unit_price: i.unit_price }))
})
```

> **project_name 自动填充**：后端在创建 SaleOrder/PurchaseOrder 时，如果传了 `project_id` 但未传 `project: name`（或为空），自动从 Project 表反查填充 `project_name`。这保证：
> 1. 旧数据查询/导出依赖 `project_name` 时不出现空值
> 2. 前端无需维护 `project_name` 冗余字段
> 3. `project_name` 始终是 `project_id` 对应项目名的快照

### 8.2 采购单新建弹窗（Purchases.vue）

同 Sales.vue，`project_name` → `project_id`，数据源切换到 `api.getProjectList()`。
后端同样自动填充 `project_name`：如果传了 `project_id` 但未传 `project_name`，从 Project 表反查。

### 8.3 项目成本添加弹窗（Projects.vue）

材料类成本：选商品+数量，金额自动计算；非材料类：直接填金额：

```vue
<el-dialog v-model="costAddVisible" title="添加项目成本" width="500px" destroy-on-close>
  <el-form label-width="80px">
    <el-form-item label="成本类型" required>
      <el-select v-model="costAddForm.cost_type" @change="onCostTypeChange">
        <el-option v-for="t in costTypes" :key="t" :label="t" :value="t" />
      </el-select>
    </el-form-item>

    <!-- 材料类：选商品+数量，金额自动计算 -->
    <template v-if="costAddForm.cost_type === '材料'">
      <el-form-item label="商品" required>
        <el-select v-model="costAddForm.product_id" filterable placeholder="选择商品" @change="onProductSelect">
          <el-option v-for="p in products" :key="p.id"
            :label="`${p.name} (库存:${p.current_stock ?? 0})`" :value="p.id" />
        </el-select>
      </el-form-item>
      <el-form-item label="数量" required>
        <el-input-number v-model="costAddForm.quantity" :min="1" @change="calcMaterialAmount" />
      </el-form-item>
      <el-form-item label="单价">
        <el-input-number v-model="costAddForm.unit_price" :min="0" :precision="2" @change="calcMaterialAmount" />
        <span style="color:#999;font-size:12px;margin-left:8px;">自动取采购价，可修改</span>
      </el-form-item>
      <el-form-item label="金额">
        <span style="font-weight:600;">¥{{ costAddForm.amount.toFixed(2) }}</span>
        <span style="color:#999;font-size:12px;margin-left:8px;">= 数量 × 单价</span>
      </el-form-item>
    </template>

    <!-- 非材料类：直接填金额 -->
    <template v-else-if="costAddForm.cost_type">
      <el-form-item label="金额" required>
        <el-input-number v-model="costAddForm.amount" :min="0" :precision="2" style="width:100%" />
      </el-form-item>
    </template>

    <el-form-item label="支付方式">
      <el-select v-model="costAddForm.payment_method">
        <el-option label="公司" value="company" />
        <el-option label="个人垫付" value="private_advance" />
      </el-select>
    </el-form-item>
    <el-form-item label="发票状态">
      <el-select v-model="costAddForm.invoice_status">
        <el-option label="未开" value="未开" />
        <el-option label="已开" value="已开" />
        <el-option label="不需开" value="不需开" />
      </el-select>
    </el-form-item>
    <el-form-item label="供应商">
      <el-input v-model="costAddForm.supplier_name" placeholder="可选" />
    </el-form-item>
    <el-form-item label="备注">
      <el-input v-model="costAddForm.notes" type="textarea" :rows="2" />
    </el-form-item>
  </el-form>
  <template #footer>
    <el-button @click="costAddVisible = false">取消</el-button>
    <el-button type="primary" @click="saveCostAdd">确认添加</el-button>
  </template>
</el-dialog>
```

对应 JavaScript：

```javascript
const costAddVisible = ref(false)
const costAddForm = ref({
  project_id: null, cost_type: '', product_id: null, quantity: 1,
  unit_price: 0, amount: 0, payment_method: 'company',
  invoice_status: '未开', supplier_name: '', notes: ''
})
const products = ref([])

const onCostTypeChange = () => {
  costAddForm.value.product_id = null
  costAddForm.value.quantity = 1
  costAddForm.value.amount = 0
  costAddForm.value.unit_price = 0
}

const onProductSelect = (productId) => {
  const p = products.value.find(x => x.id === productId)
  if (p) {
    costAddForm.value.unit_price = p.purchase_price || 0
    calcMaterialAmount()
  }
}

// 计算材料金额 = 数量 × 单价
const calcMaterialAmount = () => {
  costAddForm.value.amount = costAddForm.value.quantity * costAddForm.value.unit_price
}

const showCostAddDialog = async (projectId) => {
  costAddForm.value = {
    project_id: projectId, cost_type: '', product_id: null, quantity: 1,
    unit_price: 0, amount: 0, payment_method: 'company',
    invoice_status: '未开', supplier_name: '', notes: ''
  }
  try {
    const res = await api.getProducts({ page: 1, page_size: 1000 })
    products.value = res.items || res
  } catch (e) { products.value = [] }
  costAddVisible.value = true
}

const saveCostAdd = async () => {
  const f = costAddForm.value
  if (!f.cost_type) { ElMessage.warning('请选择成本类型'); return }
  if (f.cost_type === '材料' && !f.product_id) { ElMessage.warning('请选择商品'); return }
  if (f.amount <= 0) { ElMessage.warning('金额必须大于0'); return }
  try {
    await api.createProjectCost({
      project_id: f.project_id,
      cost_type: f.cost_type,
      amount: f.amount,
      product_id: f.cost_type === '材料' ? f.product_id : null,
      quantity: f.cost_type === '材料' ? f.quantity : null,
      payment_method: f.payment_method,
      invoice_status: f.invoice_status,
      supplier_name: f.supplier_name || null,
      notes: f.notes || null
    })
    ElMessage.success('成本添加成功')
    costAddVisible.value = false
    viewManagedDetail({ id: f.project_id })
  } catch (e) {
    ElMessage.error(e.response?.data?.detail || '添加失败')
  }
}
```

> **金额口径**：`amount` 是唯一真实值，由前端 `quantity × unit_price` 自动计算后提交。
> 后端以 `amount` 为准，不存储 `unit_price`，不校验 `amount == unit_price * quantity`。
> 原因：实际业务中金额可能包含折扣/运费分摊/手动调整，强制重算反而违反业务真实。
> 如果未来需要审计单价，可在 `project_costs` 表新增 `unit_price` 列单独存储。

### 8.4 项目详情弹窗（Projects.vue）

成本明细中增加商品/数量列和添加按钮，新增采购单 tab：

```vue
<!-- 成本明细 tab -->
<el-tab-pane label="成本明细">
  <div style="margin-bottom:8px;">
    <el-button type="primary" size="small" @click="showCostAddDialog(managedDetail.id)">添加成本</el-button>
  </div>
  <el-table :data="managedDetail.costs" style="width:100%" size="small">
    <el-table-column prop="cost_type" label="类型" width="80" />
    <el-table-column prop="product_name" label="商品" width="120">
      <template #default="scope">{{ scope.row.product_name || '-' }}</template>
    </el-table-column>
    <el-table-column prop="quantity" label="数量" width="80">
      <template #default="scope">{{ scope.row.quantity || '-' }}</template>
    </el-table-column>
    <el-table-column prop="amount" label="金额" width="100" align="right">
      <template #default="scope">{{ scope.row.amount.toFixed(2) }}</template>
    </el-table-column>
    <!-- ... 其他列保持不变 ... -->
  </el-table>
</el-tab-pane>

<!-- 关联采购单 tab -->
<el-tab-pane label="关联采购单">
  <el-table :data="managedDetail.purchases" style="width:100%" size="small">
    <el-table-column prop="order_no" label="采购单号" width="160" />
    <el-table-column prop="supplier_name" label="供应商" width="120" />
    <el-table-column prop="total_price" label="金额" width="120" align="right">
      <template #default="scope">{{ scope.row.total_price.toFixed(2) }}</template>
    </el-table-column>
    <el-table-column prop="status" label="状态" width="100">
      <template #default="scope">
        <el-tag :type="scope.row.status === 'completed' ? 'success' : 'warning'" size="small">
          {{ scope.row.status === 'completed' ? '已完成' : scope.row.status }}
        </el-tag>
      </template>
    </el-table-column>
    <el-table-column prop="purchase_date" label="日期" width="120">
      <template #default="scope">{{ scope.row.purchase_date?.slice(0, 10) }}</template>
    </el-table-column>
  </el-table>
</el-tab-pane>
```

### 8.5 成本编辑弹窗增强（Projects.vue）

扩展支持修改 product_id/quantity：

```vue
<el-dialog v-model="costEditVisible" title="编辑项目成本" width="480px" destroy-on-close>
  <el-form label-width="80px">
    <el-form-item label="金额">
      <el-input-number v-model="costEditForm.amount" :min="0" :precision="2" style="width:100%" />
    </el-form-item>
    <template v-if="costEditForm.cost_type === '材料'">
      <el-form-item label="商品">
        <el-select v-model="costEditForm.product_id" filterable style="width:100%">
          <el-option v-for="p in products" :key="p.id"
            :label="`${p.name} (库存:${p.current_stock ?? 0})`" :value="p.id" />
        </el-select>
      </el-form-item>
      <el-form-item label="数量">
        <el-input-number v-model="costEditForm.quantity" :min="1" style="width:100%" />
      </el-form-item>
    </template>
    <el-form-item label="附件图片">
      <ImageUpload v-model="costEditForm.image_url" business-type="cost"
        :record-id="costEditForm.id || 0"
        :update-api="(data) => api.updateProjectCost(costEditForm.id, data)" />
    </el-form-item>
  </el-form>
  <template #footer>
    <el-button @click="costEditVisible = false">取消</el-button>
    <el-button type="primary" @click="saveCostEdit">保存</el-button>
  </template>
</el-dialog>
```

---

## 九、改动文件清单

| 文件 | 改动内容 | 风险 |
|------|---------|------|
| `backend/models.py` | 4个表增加列 + ProjectIncome UNIQUE 约束 | 低（新增nullable列） |
| `backend/database.py` | `_migrate_linkage` 迁移函数（索引+UNIQUE+旧数据回填+收入回填+去重） | 中（一次性迁移） |
| `backend/schemas.py` | 对应 Create/Update/Out 增加字段 | 低 |
| **`backend/crud/linkage.py`** | **新建**：联动业务逻辑层（含不变量断言+幂等检查） | 低（新增文件） |
| `backend/crud/__init__.py` | 导出 linkage 函数 | 低 |
| `backend/utils.py` | `update_project_summary` 改造 + `verify_invariants` 不变量验证 | 低 |
| `backend/routers/project_costs.py` | 调用 linkage + update_project_summary，删除增量累加，单次commit | **中** |
| `backend/crud/orders.py` | 调用 linkage + update_project_summary，显式赋值 project_id，单次commit | **中** |
| `backend/routers/projects.py` | 级联回补 + 聚合接口改造 + 详情返回采购单 + 对账API | **中** |
| `frontend/src/views/Sales.vue` | project_id 下拉 + 数据源切换 | 低 |
| `frontend/src/views/Purchases.vue` | project_id 下拉 + 数据源切换 | 低 |
| `frontend/src/views/Projects.vue` | 成本添加/编辑弹窗增强 + 采购单 tab + 商品列 | 低 |

---

## 十、风险与回退

### 10.1 数据兼容性

- **project_name 保留不删** — 旧数据仍可通过文本匹配
- **project_id 默认 NULL** — 不影响已有记录
- **source_type 默认 'manual'** — 已有收入标记为手动录入
- **product_id / quantity 默认 NULL** — 已有成本记录不受影响
- **旧数据回填基于精确匹配** — 未匹配的保留 NULL 不受影响

### 10.2 回退方案

所有新增字段为 nullable，如需回退：
1. 前端切回 project_name 文本输入
2. 后端忽略 project_id，继续用 project_name
3. 库存联动逻辑可通过配置开关关闭
4. update_project_summary 可回退为仅统计 ProjectCost

### 10.3 不变量保障机制

| 不变量 | 模型层保障 | 代码层保障 | 事后校验 |
|--------|-----------|-----------|---------|
| I 库存 | — | `cost_deduct_inventory` 校验+assert | `verify_invariants` 检查负库存 |
| II 收入 | `uq_income_source` UNIQUE 索引 | `sale_create_income` 幂等检查 | `verify_invariants` 检查重复 |
| III 汇总 | — | `update_project_summary` 在 commit 前调用 | `verify_invariants` + `reconcile` 对账 |

### 10.4 注意事项

- **SQLite 事务限制**：所有联动操作 + 汇总重算必须在同一个 try/rollback 块内，一次性 commit
- **级联删除顺序**：删除 Project 时，必须先回补库存再执行 db.delete(project)
- **增量 vs 重算**：利润计算统一改为 `update_project_summary` 重算，不再使用增量累加
- **幂等安全**：`sale_create_income` 先检查再插入，UNIQUE 索引作为最终防线
- **对账时机**：日常巡检调用 `/api/projects/verify-invariants`，数据异常时调用 `/api/projects/reconcile`

---

## 十一、实施顺序

```
步骤1: models.py + database.py + schemas.py（数据层：模型+迁移+UNIQUE索引+旧数据回填）
  ↓ 验证：新列创建 + uq_income_source 索引存在 + 旧数据 project_id 已回填

步骤2: crud/linkage.py（联动业务层：含不变量断言+幂等检查）
  ↓ 验证：函数可独立调用，重复调用 sale_create_income 不报错

步骤3: utils.py（统一利润计算 + 不变量验证函数）
  ↓ 验证：update_project_summary 计算正确 + verify_invariants 能检测违规

步骤3.5: routers/projects.py（对账 API）
  ↓ 验证：/api/projects/verify-invariants 和 /api/projects/reconcile 可用

步骤4: routers/project_costs.py + crud/orders.py + routers/projects.py（路由层调用 linkage，单次commit）
  ↓ 测试：创建材料成本→库存减少+汇总一致，删除→库存回补+汇总一致
  ↓ 测试：创建销售单→项目收入自动生成（幂等），删除→联动删除
  ↓ 测试：库存不足时HTTPException，rollback后库存不变
  ↓ 调用 verify-invariants 确认三大不变量全部通过

步骤5: 前端改动（Sales.vue + Purchases.vue + Projects.vue）
  ↓ 测试：端到端验证
```

每步独立可测，出问题只回退当前步骤。

---

## 十二、进阶优化（可选）

以下优化为锦上添花，核心联动稳定后再引入：

### 11.1 SQLAlchemy event 监听自动重算

用事件监听替代手动调用 `update_project_summary`，确保不管哪个入口修改成本/收入，项目汇总自动更新：

```python
# models.py 底部
from sqlalchemy import event

@event.listens_for(ProjectCost, "after_insert")
@event.listens_for(ProjectCost, "after_update")
@event.listens_for(ProjectCost, "after_delete")
@event.listens_for(ProjectIncome, "after_insert")
@event.listens_for(ProjectIncome, "after_update")
@event.listens_for(ProjectIncome, "after_delete")
def _on_project_cost_income_change(mapper, connection, target):
    """成本/收入变更后自动重算项目汇总"""
    from utils import update_project_summary_raw
    update_project_summary_raw(connection, target.project_id)
```

```python
# utils.py — connection 版本（事件监听专用，避免 Session 冲突）
def update_project_summary_raw(connection, project_id: int):
    from sqlalchemy import text
    result = connection.execute(text("""
        SELECT
            COALESCE((SELECT SUM(amount) FROM project_incomes WHERE project_id = :pid), 0),
            COALESCE((SELECT SUM(amount) FROM project_costs WHERE project_id = :pid), 0),
            COALESCE((SELECT SUM(total_price) FROM purchase_orders WHERE project_id = :pid AND status = 'completed'), 0)
    """), {"pid": project_id}).fetchone()
    total_income, project_cost, purchase_cost = result
    total_cost = project_cost + purchase_cost
    profit = total_income - total_cost
    connection.execute(text("""
        UPDATE projects SET total_income = :ti, total_cost = :tc, profit = :p WHERE id = :pid
    """), {"ti": round(total_income, 2), "tc": round(total_cost, 2), "p": round(profit, 2), "pid": project_id})
```

引入 event 监听后，路由层可移除所有 `update_project_summary` 手动调用。

### 11.2 前端单 tab 合并

当前 Projects.vue 有双 tab（机制A + 机制B），改造后机制A 已无存在必要：
- 合并为单一的"项目看板"tab
- 每个项目展示：基本信息 + 成本明细 + 收入明细 + 关联采购单 + 关联销售单
- 删除 `routers/projects.py` 中旧的 `get_projects()` 聚合接口