---
doc-type: snapshot
---

# 浠ｇ爜璋冪敤閫昏緫鍥?

> 鏈枃妗ｈ缁嗘弿杩拌繘閿€瀛?税务+璐㈠姟绠＄悊绯荤粺涓紝鍑芥暟绾у埆鐨勪唬鐮佽皟鐢ㄥ叧绯汇€?
> 娑电洊璺敱灞?鈫?鍛戒护灞?鈫?寮曟搸灞?鈫?鍩虹璁炬柦灞傜殑瀹屾暣璋冪敤閾俱€?

---

## 1. 系统架构总览

### 1.1 分层架构

```
鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
鈹?                  FastAPI 璺敱灞?                     鈹?
鈹? sales / purchases / invoices / expenses / payments  鈹?
鈹? receipts / fixed_assets / bank_* / month_end / tax  鈹?
鈹溾攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
鈹?                  鍛戒护璋冨害灞?(CQRS)                    鈹?
鈹? Command基类 + @register瑁呴グ鍣?+ dispatch()派发       鈹?
鈹? sale_commands / purchase_commands / invoice_commands 鈹?
鈹? finance_commands / month_end / bank_reconcile        鈹?
鈹溾攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
鈹?                    涓氬姟寮曟搸灞?                       鈹?
鈹? InventoryEngine / FinanceEngine / JournalEngine     鈹?
鈹? LedgerEngine / BankEngine / BankReconcileEngine     鈹?
鈹? FixedAssetEngine / TaxAccrualEngine / TaxCheckEngine 鈹?
鈹? AccountingEngine (绾绠?                            鈹?
鈹溾攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
鈹?                   鍩虹璁炬柦灞?                         鈹?
鈹? EventBus / UoW / Database / Middleware / Handlers    鈹?
鈹? finance_integration (post_journal / reverse_journal) 鈹?
鈹? crud/reversal (冲红工具)                             鈹?
鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
```

### 1.2 鏍稿績璋冪敤娴?

```
HTTP请求 鈫?Router 鈫?dispatch(Command) 鈫?CommandHandler.handle()
    鈫?Engine.xxx() 鈫?finance_integration.post_journal()
    鈫?JournalEngine.post() 鈫?JournalEngine._build()
    鈫?JournalEngine._validate() 鈫?LedgerEngine.update_balance()
    鈫?EventBus.emit() 鈫?Handlers._emit_log()
```

### 1.3 鏁版嵁鐪熺浉婧?

| 鐪熺浉婧?| 说明 |
|--------|------|
| StockMove | 库存变动唯一来源，Inventory 涓虹紦瀛?|
| LedgerAccountBalance | 财务余额唯一来源，由 LedgerEngine 维护 |
| invoice_linkage | 发票与业务单关联，冲红时级联依赖 |

---

## 2. 鍩虹璁炬柦灞?

### 2.1 命令调度 鈥?`backend/commands/base.py`

```
@register(MyCommand)          # 瑁呴グ鍣細灏?Handler 娉ㄥ唽鍒板叏灞€ _registry 字典
class MyHandler(CommandHandler):
    def handle(self, cmd, db): ...

dispatch(cmd, db)            # 查找 _registry[cmd_type] 鈫?handler.handle(cmd, db)
  ├─ set_audit_context()     # 设置审计上下文（account_id, operator锛?
  └─ handler.handle()        # 执行业务逻辑
```

**鍏抽敭鍑芥暟锛?*
- `register(cmd_type)` 鈥?绫昏楗板櫒锛屾敞鍐?Command→Handler 鏄犲皠鍒?`_registry`
- `dispatch(cmd, db)` 鈥?查表执行：`set_audit_context()` 鈫?`handler.handle()`

### 2.2 事件总线 鈥?`backend/events.py`

```
bus = EventBus()

@bus.on("sale_order.created", priority=0)
def handler(**kwargs): ...     # 娉ㄥ唽浜嬩欢澶勭悊鍣?

bus.emit("sale_order.created", **kwargs)
  ├─ _middleware_chain()       # 中间件链前置处理
  ├─ handler_0(**kwargs)       # 鎸?priority 升序执行
  ├─ handler_1(**kwargs)
  └─ return [results]         # 杩斿洖鎵€鏈?handler 杩斿洖鍊?

bus.emit_async(...)            # 寮傛鐗堟湰锛岀敤浜?FastAPI 路由
```

**鍏抽敭鍑芥暟锛?*
- `EventBus.on(event, priority, condition)` 鈥?瑁呴グ鍣ㄦ敞鍐?handler
- `EventBus.emit(event, **kwargs)` 鈥?同步触发，按 priority 升序执行
- `EventBus.emit_async(event, **kwargs)` 鈥?异步触发

### 2.3 事务管理 鈥?`backend/uow.py`

```
with unit_of_work(db):         # 写操作：commit 鎴?rollback
    crud.create_xxx(db, ...)   # 只做 db.flush()

with read_only(db):            # 鍙鎿嶄綔锛氳嚜鍔?close
    crud.list_xxx(db, ...)
```

**鍏抽敭鍑芥暟锛?*
- `unit_of_work(db)` 鈥?上下文管理器，yield db 鈫?commit / except rollback
- `read_only(db)` 鈥?只读上下文，yield db 鈫?finally close

### 2.4 鏁版嵁搴撳畧鍗?鈥?`backend/database.py`

```
engine = create_engine(...)
SessionLocal = sessionmaker(engine)

# DDL/DML 拦截：非 ORM 写操作被拒绝
# write_permission 上下文变量控制写权限
```

**鍏抽敭鏈哄埗锛?*
- DDL/DML 拦截守卫：非 ORM 写操作被拒绝
- `write_permission` 涓婁笅鏂囧彉閲忥細鎺у埗鏄惁鍏佽鍐欐搷浣?
- `get_db()` 鈥?FastAPI 依赖注入，yield db session

### 2.5 中间件栈 鈥?`backend/middleware.py` + `backend/handlers.py`

```
# 浜嬩欢涓棿浠?
register_middleware(logging_middleware)         # 鏃ュ織涓棿浠?
register_middleware(invariant_check_middleware) # 不变量校验中间件

# 事件处理器（handlers.py锛?
@bus.on("sale_order.created")
def _emit_log(**kwargs):          # 通用日志写入：operation + entity + summary
    db.add(AuditLog(...))
```

### 2.6 财务集成 鈥?`backend/finance_integration.py`

```
post_journal(db, account_id, move_type, source, force=False)
  ├─ 骞傜瓑妫€鏌ワ細鐩稿悓 source_model+source_id 且非冲红 鈫?杩斿洖鏃у嚟璇?
  ├─ get_or_create_ledger_id(db, account_id)  # 获取或创建科目ID
  └─ JournalEngine.post()          # 调用凭证引擎

reverse_journal(db, account_id, source_model, source_id, reversal_date=None, force=False)
  ├─ 骞傜瓑妫€鏌ワ細宸插瓨鍦?is_reversal=True 鐨勫啿绾㈠嚟璇?鈫?返回 None
  ├─ 鏌ヨ鍘?AccountMove (source_model + source_id, is_reversal=False)
  ├─ 创建冲红 AccountMove (move_type=原move_type, is_reversal=True)
  鈹?  └─ BR-22: 冲红日期默认=原凭证日期，防止 BS 报表 cutoff 错配
  ├─ 逐行创建 AccountMoveLine (借贷互换)
  └─ LedgerEngine.update_balance()  # 更新科目余额
```

**鍏抽敭鍑芥暟锛?*
- `post_journal(move_type, source, db)` 鈥?缁熶竴鍑瘉鍏ュ彛锛屽鎵?`JournalEngine.post()`
- `reverse_journal(original_move_id, db)` 鈥?鍐茬孩鍑瘉锛屽€熻捶浜掓崲 + 更新科目余额
- `EXPENSE_ACCOUNT_CODE_MAP` 鈥?费用科目映射字典

### 2.7 冲红工具 鈥?`backend/crud/reversal.py`

```
reverse_receipts(sale_order_id, db)
  ├─ 查询关联 Receipt
  ├─ 逐笔创建反向 Receipt (璐熼噾棰?
  ├─ 逐笔创建反向 BankTransaction
  ├─ BankEngine.update_balance()   # 回滚银行余额
  └─ 更新 SaleOrder.payment_status

reverse_payments(purchase_order_id, db)
  ├─ 查询关联 Payment
  ├─ 逐笔创建反向 Payment (璐熼噾棰?
  ├─ 逐笔创建反向 BankTransaction
  ├─ BankEngine.update_balance()
  └─ 更新 PurchaseOrder.payment_status

reverse_single_receipt(receipt_id, db)
  ├─ 鏌ヨ鍘?Receipt
  ├─ 创建反向 Receipt + BankTransaction
  ├─ BankEngine.update_balance()
  └─ reverse_journal()             # 冲红凭证

reverse_single_payment(payment_id, db)
  ├─ 鏌ヨ鍘?Payment
  ├─ 创建反向 Payment + BankTransaction
  ├─ BankEngine.update_balance()
  └─ reverse_journal()

reverse_bank_transaction(bank_tx_id, db)
  ├─ reverse_journal()             # 冲红凭证
  ├─ 创建反向 BankTransaction
  └─ BankEngine.update_balance()
```

### 2.8 安全中间件栈 鈥?HTTP璇锋眰灞?

```
AIGatewayMiddleware        # AI缃戝叧锛氶壌鏉?+ 限流
  鈫?WritePermissionMiddleware  # 鍐欐潈闄愭牎楠?
    鈫?ReadonlyMiddleware       # 只读模式拦截
      鈫?ConfirmMiddleware      # 确认弹窗拦截
        鈫?Route Handler
```

---

## 3. 閿€鍞ā鍧楄皟鐢ㄩ摼

### 3.1 璺敱灞?鈥?`backend/routers/sales.py`

| 路由端点 | HTTP | 调用函数 |
|---------|------|---------|
| `list_sales` | GET | `crud.list_sale_orders()` + `crud.bulk_has_invoice()` |
| `create_sale` | POST | `dispatch(CreateSaleOrder)` |
| `update_sale` | PUT | 判断更新类型 鈫?`dispatch(UpdateSaleOrderItems/CancelSaleOrder/RestoreSaleOrder/UpdateSaleOrderFields)` |
| `cancel_sale` | POST | `dispatch(CancelSaleOrder)` |
| `return_sale` | POST | `dispatch(ReturnSaleOrder)` |
| `delete_sale` | DELETE | `dispatch(DeleteSaleOrder)` |

### 3.2 鍛戒护灞?鈥?`backend/commands/sale_commands.py`

```
CreateSaleOrderHandler.handle(cmd, db)
  ├─ _generate_order_no()                   # 生成单号：SO+YYYYMMDD+序号
  ├─ get_product(db, item.product_id)        # 获取商品
  ├─ SaleOrderDomain.from_orm(order)         # 领域模型转换
  鈹?  └─ .validate()                         # 鏍￠獙锛氭暟閲?0、价格≥0
  鈹?  └─ .transition_to("confirmed")         # 鐘舵€佹満锛歞raft→confirmed
  ├─ InventoryEngine.outbound(account_id, product_id, qty, ...)  # 库存出库
  鈹?  └─ 鍐?StockMove + 更新 Inventory（移动加权平均）
  ├─ FinanceEngine.record_sale(order)        # 璁板綍閿€鍞储鍔?
  鈹?  └─ post_journal(account_id, "sale_order", source)  # sale_order 凭证
  └─ bus.emit("sale_order.created")          # 触发事件

CancelSaleOrderHandler.handle(cmd, db)
  ├─ SaleOrderDomain.transition_to("cancelled")
  ├─ FinanceEngine.reverse_sale(order_id)    # 冲红财务（reverse_journal锛?
  ├─ InventoryEngine.reverse(account_id, product_id, qty, source_type="sale_order", ...)  # 搴撳瓨鍥為€€
  └─ reverse_receipts(sale_order_id, db)     # 冲红收款

ReturnSaleOrderHandler.handle(cmd, db)
  ├─ 鏍￠獙閫€璐ф暟閲?鈮?原单数量
  ├─ InventoryEngine.reverse(account_id, product_id, qty, source_id_override=return_id, ...)  # 閮ㄥ垎搴撳瓨鍥為€€
  ├─ post_journal(account_id, "sale_return", ...)  # 部分冲红凭证
  └─ 更新 SaleItem.returned_quantity

DeleteSaleOrderHandler.handle(cmd, db)
  ├─ 校验：仅draft鐘舵€佸彲鍒?
  └─ db.delete(order)
```

---

## 4. 閲囪喘妯″潡璋冪敤閾?

### 4.1 璺敱灞?鈥?`backend/routers/purchases.py`

| 路由端点 | HTTP | 调用函数 |
|---------|------|---------|
| `list_purchases` | GET | `crud.list_purchase_orders()` + `crud.bulk_has_invoice()` |
| `create_purchase` | POST | `dispatch(CreatePurchaseOrder)` |
| `update_purchase` | PUT | `dispatch(UpdatePurchaseOrderItems/CancelPurchaseOrder/RestorePurchaseOrder/UpdatePurchaseOrderFields)` |
| `cancel_purchase` | POST | `dispatch(CancelPurchaseOrder)` |
| `return_purchase` | POST | `dispatch(ReturnPurchaseOrder)` |
| `delete_purchase` | DELETE | `dispatch(DeletePurchaseOrder)` |

### 4.2 鍛戒护灞?鈥?`backend/commands/purchase_commands.py`

```
CreatePurchaseOrderHandler.handle(cmd, db)
  ├─ _generate_order_no()                   # PO+YYYYMMDD+序号
  ├─ get_product(db, item.product_id)
  ├─ PurchaseOrderDomain.from_orm(order)
  鈹?  └─ .validate() + .transition_to("confirmed")
  ├─ InventoryEngine.inbound(account_id, product_id, qty, unit_price, tax_rate, ...)  # 库存入库
  鈹?  ├─ 鍐?StockMove（入库记录）
  鈹?  ├─ 更新 Inventory 缓存（移动加权平均）
  鈹?  └─ 返回 calculated_data {total_cost, tax_amount, total_amount}
  ├─ FinanceEngine.record_purchase(order, calculated_data)
  鈹?  └─ post_journal(account_id, "purchase_order", source)  # purchase_order 凭证
  └─ bus.emit("purchase_order.created")

CancelPurchaseOrderHandler.handle(cmd, db)
  ├─ FinanceEngine.reverse_purchase(order_id)
  ├─ InventoryEngine.reverse(account_id, product_id, qty, source_type="purchase_order", ...)
  └─ reverse_payments(purchase_order_id, db)  # 冲红付款
```

---

## 5. 鍙戠エ妯″潡璋冪敤閾?

### 5.1 璺敱灞?鈥?`backend/routers/invoices.py`

| 路由端点 | HTTP | 调用函数 |
|---------|------|---------|
| `get_invoices` | GET | 绛涢€夋煡璇?+ `bulk_has_invoice()` |
| `create_invoice` | POST | `AccountingEngine.calculate_invoice_amounts()` 鈫?`dispatch(CreateInvoice)` |
| `quick_create_invoice` | POST | `AccountingEngine.calculate_invoice_amounts()` 鈫?`dispatch(CreateInvoice/CreateInvoiceWithFixedAsset)` |
| `upload_pdf` | POST | `AccountingEngine.calculate_invoice_amounts()` 鈫?`dispatch(CreateInvoice/CreateInvoiceWithFixedAsset)` |
| `certify_invoice` | POST | `dispatch(CertifyInvoice)` |
| `reverse_invoice` | POST | `dispatch(ReverseInvoice)` |
| `update_invoice` | PUT | `dispatch(UpdateInvoice)` |
| `delete_invoice` | DELETE | `dispatch(DeleteInvoice)` + 删PDF文件 |

### 5.2 鍛戒护灞?鈥?`backend/commands/invoice_commands.py`

```
CreateInvoiceHandler.handle(cmd, db)
  ├─ 鏍￠獙鍙戠エ鍙峰敮涓€鎬?
  ├─ validate_link_target()               # 校验关联目标(閿€鍞崟/閲囪喘鍗?
  ├─ validate_invoice_amounts()           # 金额等式校验
  ├─ 创建 Invoice + InvoiceItem
  └─ 若关联销售单 鈫?_auto_generate_sale_order()
      或关联采购单 鈫?_auto_generate_purchase_order()

_auto_generate_sale_order(invoice, db)
  ├─ InventoryEngine.outbound(account_id, product_id, qty, ...)  # 库存出库
  └─ FinanceEngine.record_sale(order)     # 璁伴攢鍞嚟璇?

_auto_generate_purchase_order(invoice, db)
  ├─ InventoryEngine.inbound(account_id, product_id, qty, unit_price, tax_rate, ...)  # 库存入库
  └─ FinanceEngine.record_purchase(order) # 璁伴噰璐嚟璇?

CertifyInvoiceHandler.handle(cmd, db)
  ├─ 鏍￠獙锛氫粎杩涢」涓撶エ鍙璇?
  └─ 更新 invoice.certified = True

ReverseInvoiceHandler.handle(cmd, db)
  ├─ 鏍囪鍘熷彂绁?is_reversed = True
  ├─ 创建红字发票 (璐熼噾棰?
  ├─ 绾ц仈鍐茬孩锛?
  鈹?  ├─ 鍏宠仈閿€鍞崟 鈫?FinanceEngine.reverse_sale() + InventoryEngine.reverse()
  鈹?  ├─ 鍏宠仈閲囪喘鍗?鈫?FinanceEngine.reverse_purchase() + InventoryEngine.reverse()
  鈹?  └─ 閮ㄥ垎閫€璐?鈫?鎸夊墿浣欓噺鍐茬孩锛堥潪鏁村崟鍐茬孩锛?
  └─ reverse_receipts() / reverse_payments()  # 鍐茬孩鏀朵粯娆?

CreateInvoiceWithFixedAssetHandler.handle(cmd, db)
  ├─ 鍘熷瓙鎿嶄綔锛氬垱寤哄彂绁?+ 创建固定资产
  └─ 涓よ€呭湪鍚屼竴浜嬪姟涓?

UpdateAssetWithInvoiceHandler.handle(cmd, db)
  ├─ 更新发票金额
  └─ 鑱斿姩鏇存柊鍥哄畾璧勪骇鍘熷€?
```

---

## 6. 璐圭敤妯″潡璋冪敤閾?

### 6.1 璺敱灞?鈥?`backend/routers/expenses.py`

| 路由端点 | HTTP | 调用函数 |
|---------|------|---------|
| `get_expenses` | GET | `crud.list_expenses()` + `bulk_has_invoice()` |
| `create_expense` | POST | `post_journal("expense")` + `crud._log()` |
| `reverse_expense` | POST | `reverse_journal("expense")` + 标记 `is_reversed` |
| `update_expense` | PUT | 鐩存帴鏇存柊锛堟棤鍑瘉鑱斿姩锛?|
| `delete_expense` | DELETE | 直接删除 |

### 6.2 费用凭证生成

```
create_expense(db, expense_in)
  ├─ EXPENSE_ACCOUNT_CODE_MAP[category]  # 费用科目映射
  ├─ post_journal("expense", source)      # 生成费用凭证
  鈹?  └─ JournalEngine.post()
  鈹?      └─ _build_expense()            # 借：费用科目 / 贷：银行/现金
  └─ crud._log()                          # 审计日志

reverse_expense(db, expense_id)
  ├─ reverse_journal(original_move_id)    # 冲红凭证
  └─ 更新 expense.is_reversed = True
```

---

## 7. 收付款模块调用链

### 7.1 付款 鈥?`backend/routers/payments.py`

```
create_payment(db, payment_in)
  ├─ 创建 Payment 记录
  ├─ 创建 BankTransaction (出账)
  ├─ BankEngine.update_balance()          # 更新银行余额
  ├─ post_journal("payment", source)      # 生成付款凭证
  鈹?  └─ JournalEngine._build_payment()   # 借：应付账款 / 贷：银行存款
  └─ 更新 PurchaseOrder.payment_status

reverse_payment(db, payment_id)
  └─ reverse_single_payment(payment_id, db)  # 瑙佸啿绾㈠伐鍏?
```

### 7.2 收款 鈥?`backend/routers/receipts.py`

```
create_receipt(db, receipt_in)
  ├─ 创建 Receipt 记录
  ├─ 创建 BankTransaction (入账)
  ├─ BankEngine.update_balance()          # 更新银行余额
  ├─ post_journal("receipt", source)      # 生成收款凭证
  鈹?  └─ JournalEngine._build_receipt()   # 借：银行存款 / 贷：应收账款
  └─ 更新 SaleOrder.payment_status

reverse_receipt(db, receipt_id)
  └─ reverse_single_receipt(receipt_id, db)  # 瑙佸啿绾㈠伐鍏?
```

---

## 8. 閾惰妯″潡璋冪敤閾?

### 8.1 银行账户 鈥?`backend/routers/bank_accounts.py`

```
create_bank_account(db, account_in)
  ├─ 创建 BankAccount
  └─ 反向同步期初余额（若存在 OpeningBalance锛?

update_bank_account(db, account_id)
  ├─ 校验：开户后禁止直接修改期初余额
  └─ 更新 BankAccount

list_bank_accounts(db)
  └─ 查询 BankAccount 鍒楄〃锛堝惈浣欓锛?
```

### 8.2 银行交易 鈥?`backend/routers/bank_transactions.py`

```
create_bank_transaction(db, tx_in)
  ├─ with_for_update() 行锁              # 防止并发问题
  ├─ 浣欓鏍￠獙锛堢姝㈤€忔敮锛?
  ├─ 创建 BankTransaction
  └─ BankEngine.update_balance()          # 更新余额

reverse_bank_transaction_endpoint(db, tx_id)
  └─ reverse_bank_transaction(tx_id, db)  # 瑙佸啿绾㈠伐鍏?
```

### 8.3 银行对账 鈥?`backend/routers/bank_reconcile.py`

| 路由端点 | HTTP | 调用函数 |
|---------|------|---------|
| `import_bank_statement` | POST | `dispatch(ImportBankStatement)` |
| `reconcile_bank` | POST | `dispatch(ReconcileBank)` |
| `force_match` | POST | `dispatch(ForceMatchBankReconciliation)` |
| `confirm` | POST | `dispatch(ConfirmBankReconciliation)` |
| `generate_entry` | POST | `dispatch(GenerateReconciliationEntry)` |
| `create_bank_entry` | POST | `post_journal("bank_fee_entry")` + 创建BankTransaction |

### 8.4 银行对账命令 鈥?`backend/commands/bank_reconcile.py`

```
ImportBankStatementHandler.handle(cmd, db)
  ├─ 创建 BankStatement
  └─ 逐行创建 BankStatementLine

ReconcileBankHandler.handle(cmd, db)
  └─ BankReconcileEngine.auto_match(db, statement_id)
      ├─ 鎸夐噾棰?日期模糊匹配
      └─ 创建 BankReconciliation + ReconciliationItem

ForceMatchBankReconciliationHandler.handle(cmd, db)
  └─ BankReconcileEngine.force_match(db, ...)

ConfirmBankReconciliationHandler.handle(cmd, db)
  └─ BankReconcileEngine.confirm(db, reconciliation_id)
      ├─ 标记 BankStatementLine.matched = True
      └─ 标记 BankReconciliation.status = "confirmed"

GenerateReconciliationEntryHandler.handle(cmd, db)
  └─ BankReconcileEngine.generate_entry(db, reconciliation_id)
      └─ post_journal("bank_reconcile")   # 生成银行对账凭证
```

---

## 9. 鍥哄畾璧勪骇妯″潡璋冪敤閾?

### 9.1 璺敱灞?鈥?`backend/routers/fixed_assets.py`

| 路由端点 | HTTP | 调用函数 |
|---------|------|---------|
| `depreciate` | POST | `FixedAssetEngine.record_depreciation()` |
| `batch_depreciate` | POST | `FixedAssetEngine.batch_depreciate()` |
| `dispose` | POST | `FixedAssetEngine.record_disposal()` |
| `update_asset_with_invoice` | PUT | `dispatch(UpdateAssetWithInvoice)` |

### 9.2 寮曟搸灞?鈥?`backend/engine_fixed_asset.py`

```
FixedAssetEngine.record_depreciation(asset_id, period)
  ├─ calculate_monthly(asset)              # 年限平均法：(鍘熷€?娈嬪€?/使用月数
  ├─ 骞傜瓑妫€鏌ワ細鍚?asset_id+period 的折旧流水是否已存在
  ├─ 鍐?FixedAssetDepreciation 流水        # 鎶樻棫鐪熺浉婧?
  ├─ 更新 FixedAsset.accumulated_depreciation  # 缓存更新
  └─ post_journal(db, account_id, "depreciation", source)  # 鍊?6601(管理费用) / 璐?1602(累计折旧)

FixedAssetEngine.batch_depreciate(period)
  └─ 閬嶅巻鎵€鏈夊湪鐢ㄨ祫浜?鈫?record_depreciation()

FixedAssetEngine.record_disposal(asset_id, disposal_price, disposal_date, bank_account_id)
  ├─ calculate_monthly(asset)              # 鏈€鍚庝竴娆℃姌鏃?
  ├─ 计算处置损益 = disposal_price - (鍘熷€?- 累计折旧)
  ├─ post_journal(db, account_id, "asset_disposal", source)  # 鍑瘉琛? 鍊?602/鍊?002 璐?601 + 损益
  └─ 鑻?disposal_price > 0 且有 bank_account_id:
      ├─ 创建 BankTransaction (inflow, flow_category="investing")
      └─ BankEngine.update_balance()       # BankAccount.balance += disposal_price
```

---

## 10. 鏈熷垵浣欓妯″潡璋冪敤閾?

### 10.1 璺敱灞?鈥?`backend/routers/opening_balances.py`

```
create_opening_balance(db, balance_in)
  └─ dispatch(CreateOpeningBalance)

update_opening_balance(db, balance_id)
  ├─ 校验：存在业务数据时禁止修改
  └─ dispatch(UpdateOpeningBalance)

delete_opening_balance(db, balance_id)
  └─ 校验：存在业务数据时禁止删除
```

### 10.2 鍛戒护灞?鈥?`backend/commands/finance_commands.py`

```
CreateOpeningBalanceHandler.handle(cmd, db)
  ├─ 校验日期唯一性（每月只能有一个期初）
  ├─ 校验资产 = 璐熷€?+ 鏉冪泭锛堜細璁℃亽绛夊紡锛?
  ├─ post_journal("opening_balance")       # 生成期初凭证
  └─ 同步 BankAccount 余额                 # 反向同步银行账户

UpdateOpeningBalanceHandler.handle(cmd, db)
  ├─ 校验：无业务数据时才允许修改
  ├─ reverse_journal(鍘熷嚟璇?               # 鍐茬孩鍘熷嚟璇?
  └─ post_journal("opening_balance")       # 重新生成

CreateCashFlowTransactionHandler.handle(cmd, db)
  ├─ post_journal("cash_flow")             # 鐢熸垚鐜伴噾娴佸嚟璇?
  └─ 创建 CashFlowTransaction
```

---

## 11. 鏈堢粨妯″潡璋冪敤閾?

### 11.1 璺敱灞?鈥?`backend/routers/month_end.py`

```
month_end_close(db, account_id, year, month)
  └─ dispatch(MonthEndClose)
```

### 11.2 鍛戒护灞?鈥?`backend/commands/month_end.py`

```
MonthEndCloseHandler.handle(cmd, db)
  ├─ 鍓嶇疆妫€鏌ワ細閾惰瀵硅处鏄惁鍏ㄩ儴 confirmed
  鈹?  └─ 查询未确认的 BankReconciliation 鈫?拒绝月结
  ├─ FixedAssetEngine.batch_depreciate(period)     # 固定资产折旧
  ├─ TaxAccrualEngine.execute(account_id, period, taxpayer_type)  # 税务计提
  └─ TaxCheckEngine.execute(db, account_id, year, month)    # 税务核对
```

---

## 12. 绋庡姟妯″潡璋冪敤閾?

### 12.1 璺敱灞?鈥?`backend/routers/tax.py`

```
get_tax_report(db, account_id, year, month)
  ├─ aggregate_vat_invoices()              # 姹囨€诲鍊肩◣鍙戠エ
  └─ AccountingEngine.calculate_vat()      # 璁＄畻澧炲€肩◣

get_tax_report_monthly(db, account_id, year)
  └─ 鍚屼笂锛屾寜鏈堣仛鍚?
```

### 12.2 税务计提引擎 鈥?`backend/engine_tax.py`

```
TaxAccrualEngine.execute(account_id, period, taxpayer_type)
  ├─ _l() / _lp()                         # 杈呭姪鍑芥暟锛氭煡璇㈢鐩€熻捶绱
  ├─ 璁＄畻澧炲€肩◣锛?
  鈹?  ├─ 涓€鑸撼绋庝汉锛氶攢椤?- 杩涢」锛堝凡璁よ瘉锛?
  鈹?  └─ 灏忚妯＄撼绋庝汉锛氬惈绋庢敹鍏?× 寰佹敹鐜?- 减免税额
  ├─ 计算附加税：
  鈹?  └─ 鍩庡缓绋?7%) + 鏁欒偛璐归檮鍔?3%) + 地方教育附加(2%)
  鈹?      基数 = 瀹炵即澧炲€肩◣
  ├─ 璁＄畻浼佷笟鎵€寰楃◣锛?
  鈹?  ├─ 灏忓井浼佷笟锛氬簲绾崇◣鎵€寰楅 × 5%
  鈹?  └─ 涓€鑸紒涓氾細搴旂撼绋庢墍寰楅 × 25%
  └─ post_journal("tax_accrual")           # 生成税务凭证
```

### 12.3 税务核对引擎 鈥?`backend/engine_tax_check.py`

```
TaxCheckEngine.execute(db, account_id, year, month)
  ├─ 核对1：应交增值税-閿€椤?vs 主营业务收入×税率
  ├─ 核对2：应交增值税-进项 vs 认证发票税额合计
  ├─ 核对3：附加税 vs 瀹炵即澧炲€肩◣脳璐圭巼
  ├─ 核对4：企业所得税 vs 利润总额×税率
  ├─ 核对5锛氳繘椤硅浆鍑?vs 闈炲簲绋庨」鐩?
  └─ 核对6：免税销售额 vs 免税政策
```

### 12.4 计算引擎 鈥?`backend/accounting_engine.py`

```
AccountingEngine.calculate_invoice_amounts(data)
  ├─ 价税分离：不含税金额 = 含税金额 / (1 + 税率)
  └─ ROUND_HALF_UP 精度控制

AccountingEngine.calculate_vat(data, taxpayer_type)
  ├─ 涓€鑸撼绋庝汉锛氶攢椤圭◣棰?- 进项税额
  └─ 小规模纳税人：含税销售额 × 寰佹敹鐜?/ (1+寰佹敹鐜?
      + 鍑忓厤闂ㄦ鍒ゆ柇锛堟湀鈮?0万免征）

AccountingEngine.calculate_income_tax(profit, is_small)
  ├─ 灏忓井锛氬埄娑?× 5%
  └─ 涓€鑸細鍒╂鼎 × 25%

AccountingEngine.calculate_depreciation_straight_line(cost, salvage, months)
  └─ (cost - salvage) / months

AccountingEngine.calculate_depreciation_double_declining(...)
  └─ 鍙屽€嶄綑棰濋€掑噺娉?

AccountingEngine.calculate_depreciation_sum_of_years(...)
  └─ 骞存暟鎬诲拰娉?

AccountingEngine.calculate_intangible_amortization(...)
  └─ 无形资产直线摊销
```

---

## 13. 璐㈠姟鎶ヨ〃妯″潡璋冪敤閾?

### 13.1 璺敱灞?鈥?`backend/routers/financial_reports.py`

```
balance_sheet(db, account_id, year, month)
  └─ crud.generate_balance_sheet()         # 浠?LedgerAccountBalance 姹囨€?

income_statement(db, account_id, year, month)
  └─ crud.generate_income_statement()      # 浠?AccountMoveLine 姹囨€?

financial_summary(db, account_id, year, month)
  ├─ balance_sheet()                       # 璧勪骇璐熷€鸿〃
  └─ opening_balance()                     # 期初余额对比
```

---

## 14. 寰€鏉ュ璐︽ā鍧楄皟鐢ㄩ摼

### 14.1 璺敱灞?鈥?`backend/routers/reconciliations.py`

```
get_partner_reconciliation(db, partner_type, partner_id)
  └─ _calc_partner_reconciliation()
      ├─ 渚涘簲鍟?瀹㈡埛姹囨€伙細
      鈹?  ├─ 期初余额（OpeningBalance锛?
      鈹?  ├─ 本期发生（PurchaseOrder/SaleOrder 閲戦鍚堣锛?
      鈹?  ├─ 已付/已收（Payment/Receipt 閲戦鍚堣锛?
      鈹?  └─ 期末余额 = 期初 + 本期 - 已付/已收
      └─ 发票明细（Invoice 閲戦锛?
```

---

## 15. 寮曟搸灞傝缁嗚皟鐢ㄥ叧绯?

### 15.1 库存引擎 鈥?`backend/engine_inventory.py`

```
InventoryEngine.inbound(account_id, product_id, quantity, unit_price, tax_rate, ...)
  ├─ 鍐?StockMove (type="in")              # 入库记录
  ├─ 更新 Inventory 缓存                   # 数量+浠峰€肩疮璁★紙绉诲姩鍔犳潈骞冲潎锛?
  └─ 返回 calculated_data {total_cost, tax_amount, total_amount}

InventoryEngine.outbound(account_id, product_id, quantity, ...)
  ├─ 计算出库成本（从 Inventory 鍙?average_cost锛?
  ├─ 鍐?StockMove (type="out")
  ├─ 更新 Inventory 缂撳瓨锛堟墸鍑忔暟閲忓拰浠峰€硷級
  └─ 返回 calculated_data {unit_cost, total_cost}

InventoryEngine.force_outbound(account_id, product_id, quantity, ...)
  └─ 鍚?outbound锛屼絾鍏佽璐熷簱瀛?

InventoryEngine.reverse(account_id, product_id, quantity, unit_cost, source_type, source_id, ...)
  ├─ 查询原始 StockMove
  ├─ 判断方向：原始正(入库)鈫掑啿閿€璐?出库)，原始负(出库)鈫掑啿閿€姝?入库)
  ├─ 鍐欏弽鍚?StockMove (source_type="{type}_reversal")
  └─ 更新 Inventory 缓存
```

### 15.2 财务引擎 鈥?`backend/engine_finance.py`

```
FinanceEngine.record_sale(order, force=False)
  ├─ _account_config()                     # 获取科目配置（纳税人类型、增值税率）
  鈹?  └─ _vat_deduction() 鈫?enable_vat_deduction
  鈹?  └─ _vat_rate() 鈫?vat_rate
  ├─ 逐行计算价税分离（直接用 item.tax_rate锛屼笉鍐嶈鐩?3%锛?
  └─ post_journal(self.db, account_id, "sale_order", source, force=force)  # 閿€鍞嚟璇?
      └─ JournalEngine._build_sale_order() # 借：应收账款 / 贷：主营业务收入+应交税费

FinanceEngine.record_purchase(order, calculated_data=None, force=False)
  ├─ _account_config()                     # 获取科目配置
  鈹?  └─ _vat_deduction() 鈫?enable_vat_deduction
  ├─ 优先使用 calculated_data（从 InventoryEngine.inbound 浼犲叆锛?
  鈹?  └─ 鏃?calculated_data 时从 order.items 计算
  └─ post_journal(db, account_id, "purchase_order", source, force=force)  # 采购凭证
      └─ JournalEngine._build_purchase_order()  # 借：库存商品+应交税费 / 贷：应付账款

FinanceEngine.reverse_sale(order_id, force=False)
  └─ reverse_journal(self.db, account_id, "sale_order", order_id, force=force)  # 鍐茬孩閿€鍞嚟璇?

FinanceEngine.reverse_purchase(order_id, force=False)
  └─ reverse_journal(self.db, account_id, "purchase_order", order_id, force=force)  # 冲红采购凭证
```

### 15.3 凭证引擎 鈥?`backend/engine_journal.py`

```
JournalEngine.post(ledger_id, move_type, source)
  ├─ _build(move_type, source)             # 按类型构建凭证行
  鈹?  ├─ _build_sale_order()               # 閿€鍞嚟璇?(SALE)
  鈹?  ├─ _build_purchase_order()           # 采购凭证 (PURCHASE)
  鈹?  ├─ _build_payment()                  # 付款凭证 (BNK)
  鈹?  ├─ _build_receipt()                  # 收款凭证 (BNK)
  鈹?  ├─ _build_expense()                  # 费用凭证 (GEN)
  鈹?  ├─ _build_opening_balance()          # 期初凭证 (GEN)
  鈹?  ├─ _build_cash_flow()                # 鐜伴噾娴佸嚟璇?(GEN)
  鈹?  ├─ _build_depreciation()             # 折旧凭证 (FA): 鍊?601 璐?602
  鈹?  ├─ _build_asset_disposal()           # 资产处置凭证 (FA)
  鈹?  ├─ _build_fixed_asset_purchase()     # 固定资产入账凭证 (GEN)
  鈹?  ├─ _build_intangible_asset_purchase() # 无形资产入账凭证 (GEN)
  鈹?  ├─ _build_sale_return()              # 閿€鍞€€璐у啿绾㈠嚟璇?(SRET)
  鈹?  ├─ _build_purchase_return()          # 閲囪喘閫€璐у啿绾㈠嚟璇?(PRET)
  鈹?  ├─ _build_tax_surcharge()            # 闄勫姞绋庤鎻愬嚟璇?(TAX): 鍊?403 璐?22104
  鈹?  ├─ _build_tax_income()               # 鎵€寰楃◣璁℃彁鍑瘉 (TAX): 鍊?801 璐?22105
  鈹?  ├─ _build_tax_income_reversal()      # 鎵€寰楃◣鍐插洖鍑瘉 (TAX)
  鈹?  ├─ _build_vat_transfer_out()         # 杞嚭鏈氦澧炲€肩◣ (VAT): 鍊?22106 璐?22107
  鈹?  ├─ _build_vat_exemption()            # 澧炲€肩◣鍑忓厤缁撹浆 (VAT): 鍊?22103 璐?301
  鈹?  ├─ _build_bank_fee_entry()           # 閾惰鎵嬬画璐?利息凭证 (BNK)
  鈹?  ├─ _build_personal_advance()         # 个人垫付凭证 (GEN): 鍊熻垂鐢ㄧ鐩?璐?241
  鈹?  └─ _build_personal_advance_repay()   # 偿还个人垫付凭证 (BNK)
  ├─ _validate(lines)                      # 鍊熻捶骞宠　鏍￠獙锛氬€熸柟鍚堣 = 贷方合计
  ├─ generate_voucher_number(db)           # 鐢熸垚鍑瘉鍙?
  ├─ 创建 AccountMove
  ├─ 逐行创建 AccountMoveLine
  └─ LedgerEngine.update_balance(db, line) # 每行更新科目余额
```

### 15.4 科目余额引擎 鈥?`backend/engine_ledger.py`

```
LedgerEngine.update_balance(line)
  ├─ 校验科目为叶子节点（非叶子科目禁止直接记账）
  ├─ _ensure_balance_row(ledger_account_id)  # 确保余额行存在（with_for_update 琛岄攣锛?
  ├─ delta = line.debit - line.credit
  ├─ 特殊校验: 1001(库存现金) balance + delta < 0 鈫?鎶?INSUFFICIENT_BALANCE
  └─ 统一公式更新（不区分科目类型）：
      ├─ balance += delta (= debit - credit)
      ├─ debit_total += line.debit
      └─ credit_total += line.credit
```

### 15.5 银行引擎 鈥?`backend/engine_bank.py`

```
BankEngine.update_balance(self, bank_account_id, amount, transaction_type)
  ├─ 查询 BankAccount (with_for_update 行锁)
  ├─ transaction_type="inflow" 鈫?balance += amount
  ├─ transaction_type="outflow" 鈫?balance -= amount
  └─ flush() 鈫?返回 BankAccount
```

### 15.6 银行对账引擎 鈥?`backend/engine_bank_reconcile.py`

```
BankReconcileEngine.auto_match(db, statement_id)
  ├─ 遍历 BankStatementLine
  ├─ 涓?BankTransaction 鎸夐噾棰?日期模糊匹配
  └─ 创建 BankReconciliation + ReconciliationItem

BankReconcileEngine.reconcile(db, reconciliation_id)
  ├─ 鏇存柊鍖归厤鐘舵€?
  └─ 更新 ReconciliationItem

BankReconcileEngine.confirm(db, reconciliation_id)
  ├─ 标记 BankStatementLine.matched = True
  └─ 标记 BankReconciliation.status = "confirmed"

BankReconcileEngine.generate_entry(db, reconciliation_id)
  └─ post_journal(db, account_id, "bank_fee_entry", source)  # 鐢熸垚閾惰鎵嬬画璐?利息凭证
```

---

## 16. 特殊流程

### 16.1 冲红机制

```
鏁村崟鍙栨秷锛?
  CancelSaleOrder 鈫?FinanceEngine.reverse_sale() + InventoryEngine.reverse()
                  + reverse_receipts() + EventBus.emit()

閮ㄥ垎閫€璐э細
  ReturnSaleOrder 鈫?浠呭啿绾㈤€€璐ч儴鍒嗙殑鏁伴噺
                  鈫?更新 SaleItem.returned_quantity

鍙戠エ鍐茬孩锛堢骇鑱旓級锛?
  ReverseInvoice 鈫?鏍囪鍘熷彂绁?is_reversed
                鈫?创建红字发票
                鈫?妫€鏌ュ凡閮ㄥ垎閫€璐ф暟閲?
                鈫?鎸夊墿浣欓噺鍐茬孩锛堥潪鏁村崟锛?
                鈫?级联：FinanceEngine.reverse + InventoryEngine.reverse
                鈫?级联：reverse_receipts / reverse_payments

璐圭敤鍐茬孩锛?
  reverse_expense 鈫?reverse_journal() + is_reversed = True

收付款冲红：
  reverse_single_receipt 鈫?反向Receipt + 反向BankTransaction
                        + BankEngine.update_balance() + reverse_journal()
  reverse_single_payment 鈫?反向Payment + 反向BankTransaction
                        + BankEngine.update_balance() + reverse_journal()

閾惰浜ゆ槗鍐茬孩锛?
  reverse_bank_transaction 鈫?reverse_journal() + 反向BankTransaction
                          + BankEngine.update_balance()
```

### 16.2 月结流程

```
MonthEndClose:
  1. 鍓嶇疆妫€鏌ワ細鎵€鏈夐摱琛屽璐﹀凡 confirmed
  2. FixedAssetEngine.batch_depreciate()    # 固定资产折旧计提
  3. TaxAccrualEngine.execute()             # 税务计提（增值税+闄勫姞绋?鎵€寰楃◣锛?
  4. TaxCheckEngine.execute()               # 绋庡姟鏍稿锛?项账表核对）
```

### 16.3 银行对账流程

```
1. ImportBankStatement  鈫?导入银行对账单（BankStatement + BankStatementLine锛?
2. ReconcileBank        鈫?鑷姩鍖归厤锛堥噾棰?鏃ユ湡妯＄硦鍖归厤锛?
3. ForceMatch           鈫?手动强制匹配
4. ConfirmBankReconciliation 鈫?确认对账结果
5. GenerateReconciliationEntry 鈫?生成对账凭证
```

### 16.4 发票创建联动流程

```
CreateInvoice:
  ├─ 若关联销售单 鈫?_auto_generate_sale_order()
  鈹?  ├─ InventoryEngine.outbound()         # 自动出库
  鈹?  └─ FinanceEngine.record_sale()        # 鑷姩璁伴攢鍞嚟璇?
  └─ 若关联采购单 鈫?_auto_generate_purchase_order()
      ├─ InventoryEngine.inbound()          # 自动入库
      └─ FinanceEngine.record_purchase()    # 鑷姩璁伴噰璐嚟璇?

CreateInvoiceWithFixedAsset:
  └─ 原子创建发票+鍥哄畾璧勪骇锛堝悓涓€浜嬪姟锛?
```

---

## 17. 数据模型关系

### 17.1 核心业务模型 鈥?`backend/models.py`

```
User 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 账套/用户
  鈹?
  鈹溾攢鈹€ Account 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 会计主体
  鈹?    鈹溾攢鈹€ Product 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 商品
  鈹?    鈹溾攢鈹€ SaleOrder 鈹€鈹€ SaleItem 鈹€鈹€鈹€鈹€鈹€鈹€ 閿€鍞崟
  鈹?    鈹溾攢鈹€ PurchaseOrder 鈹€鈹€ PurchaseItem 閲囪喘鍗?
  鈹?    鈹溾攢鈹€ Invoice 鈹€鈹€ InvoiceItem 鈹€鈹€鈹€鈹€ 发票
  鈹?    鈹溾攢鈹€ Expense 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 费用
  鈹?    鈹溾攢鈹€ Payment 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 付款
  鈹?    鈹溾攢鈹€ Receipt 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 收款
  鈹?    鈹溾攢鈹€ FixedAsset 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 固定资产
  鈹?    鈹溾攢鈹€ OpeningBalance 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 期初余额
  鈹?    鈹溾攢鈹€ CashFlowTransaction 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 鐜伴噾娴?
  鈹?    鈹溾攢鈹€ Supplier 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 渚涘簲鍟?
  鈹?    鈹溾攢鈹€ Customer 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 客户
  鈹?    鈹溾攢鈹€ StockMove 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 库存变动(鐪熺浉婧?
  鈹?    鈹斺攢鈹€ Inventory 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 库存缓存
  鈹?
  鈹斺攢鈹€ BankAccount 鈹€鈹€ BankTransaction 鈹€鈹€ 银行账户/交易
```

### 17.2 财务模型 鈥?`backend/models_finance.py`

```
Ledger 鈹€鈹€ LedgerAccount 鈹€鈹€ LedgerAccountBalance   总账/科目/余额(鐪熺浉婧?
AccountJournal 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 鍑瘉瀛?
AccountMove 鈹€鈹€ AccountMoveLine 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€  凭证/凭证明细
```

### 17.3 银行对账模型 鈥?`backend/models_bank.py`

```
BankStatement 鈹€鈹€ BankStatementLine 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 閾惰瀵硅处鍗?
BankReconciliation 鈹€鈹€ ReconciliationItem 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€ 对账/对账明细
```

---

## 18. 前端调用后端API映射

### 18.1 API端点总览

| 前端页面 | 后端路由前缀 | 核心端点 |
|---------|-------------|---------|
| 閿€鍞鐞?| `/api/sales` | list, create, update, cancel, return, delete |
| 采购管理 | `/api/purchases` | list, create, update, cancel, return, delete |
| 发票管理 | `/api/invoices` | list, create, quick_create, upload_pdf, certify, reverse, update, delete |
| 费用管理 | `/api/expenses` | list, create, reverse, update, delete |
| 付款管理 | `/api/payments` | list, create, reverse |
| 收款管理 | `/api/receipts` | list, create, reverse |
| 固定资产 | `/api/fixed-assets` | list, depreciate, batch_depreciate, dispose, update_with_invoice |
| 银行账户 | `/api/bank-accounts` | list, create, update |
| 银行交易 | `/api/bank-transactions` | list, create, reverse |
| 银行对账 | `/api/bank-reconcile` | import, reconcile, force_match, confirm, generate_entry, create_entry |
| 寰€鏉ュ璐?| `/api/reconciliations` | get_partner |
| 期初余额 | `/api/opening-balances` | list, create, update, delete |
| 财务报表 | `/api/financial-reports` | balance_sheet, income_statement, summary |
| 月末结账 | `/api/month-end` | close |
| 税务报表 | `/api/tax` | report, report_monthly |
