---
doc-type: snapshot
---

# 鏁版嵁鍥犳灉閾?

> 鏈枃妗ｅ熀浜庛€婁唬鐮佽皟鐢ㄩ€昏緫鍥俱€嬫彁鐐硷紝鎻忚堪绯荤粺涓暟鎹彉鏇寸殑鍥犳灉浼犳挱璺緞銆?
> 姣忔潯鍥犳灉閾捐拷韪竴涓笟鍔″姩浣滀粠婧愬ご鍒版渶缁堟暟鎹惤鍦扮殑瀹屾暣褰卞搷闈€?
> 鍚屾椂鏍囨敞浜嗘枃妗ｉ獙璇佷腑鍙戠幇鐨勪笉涓€鑷翠箣澶勩€?

---

## 0. 文档验证结果

> **更新**: 2026-06-30 鈥?对照 `浠ｇ爜璋冪敤閫昏緫鍥?md` v2026-06-30 閲嶉獙銆?
> 鍘?10 项中 8 椤瑰凡鍦ㄤ唬鐮佽皟鐢ㄩ€昏緫鍥句腑淇锛? 项部分修复；1 椤瑰瓨鐤戙€傛柊澧?3 椤逛笉涓€鑷淬€?

### 鍘?10 椤圭姸鎬?

| # | 鍘熸弿杩?| 2026-06-30 鐘舵€?|
|---|--------|----------------|
| 1 | move_type `"sale"` vs `"sale_order"` | 鉁?鍥惧凡淇锛?5.2 节用 `"sale_order"`锛?|
| 2 | move_type `"purchase"` vs `"purchase_order"` | 鉁?鍥惧凡淇锛?5.2 节用 `"purchase_order"`锛?|
| 3 | move_type `"bank_reconcile"` vs `"bank_fee_entry"` | 鉁?鍥惧凡淇锛?.4 鑺傛敼鐢?`"bank_fee_entry"`锛?|
| 4 | 折旧 comment 6602 vs 6601 | ⚠️ 部分修复：engine_journal.py 娉ㄩ噴宸叉敼涓?6601，但 engine_fixed_asset.py:52 docstring 仍写 `鍊?6602` |
| 5 | 鍥惧垪鍑?5 涓笉瀛樺湪鐨?builder | 鉂?鍥惧綋鍓?15.3 鑺傛湭鍒楀嚭杩?5 涓紝鍙兘宸蹭慨姝ｆ垨鍘熻褰曟湁璇?|
| 6 | LedgerEngine 鍒?绫?vs 统一公式 | 鉁?鍥惧凡淇锛?5.4 鑺傛樉绀虹粺涓€鍏紡锛?|
| 7 | reverse_journal .reversal 后缀 | 鉁?鍥惧凡淇锛?.6 鑺傛樉绀?is_reversal=True锛?|
| 8 | 缺少 12 涓?builder | 鉁?鍥惧凡琛ュ叏锛?5.3 鑺傚垪鍑哄叏閮?21 个） |
| 9 | TaxAccrualEngine 参数 (db, year, month) | 鉁?鍥惧凡淇锛?2.2 节显示正确签名） |
| 10 | BankEngine.update_balance 参数 (db, id) | 鉁?鍥惧凡淇锛?5.5 节显示正确签名） |

### 新增不一致（2026-06-30 鍙戠幇锛?

| # | 鍥炬弿杩?| 源码实际 | 涓ラ噸搴?|
|---|--------|---------|--------|
| 11 | InventoryEngine.inbound 参数 `(db, items, order_type, order_id)`锛?5.1 节） | 实际签名 `(self, account_id, product_id, quantity, unit_price, tax_rate, ...)` 瀹屽叏涓嶅悓鐨勬帴鍙?| **楂?* |
| 12 | InventoryEngine.outbound 参数 `(db, items, order_type, order_id)`锛?5.1 节） | 同上，完全不同的接口 | **楂?* |
| 13 | FinanceEngine.record_sale / record_purchase / reverse_sale / reverse_purchase 鍚?`db` 鍙傛暟锛?5.2 节） | 瀹為檯鏃?`db` 参数，db 閫氳繃鏋勯€犲嚱鏁颁紶鍏ワ紙`self.db`锛?| **涓?* | |

---

## 1. 閿€鍞垱寤哄洜鏋滈摼

```
用户操作: 鍒涘缓閿€鍞崟
    鈹?
    鈻?
CreateSaleOrderHandler.handle()
    鈹?
    ├─[因] 创建 SaleOrder + SaleItem
    鈹?    └─[果] SaleOrder.status = PENDING 鈫?CONFIRMED
    鈹?
    ├─[因] InventoryEngine.outbound(account_id, product_id, qty, ...)
    鈹?    ├─[果] StockMove(type=out, quantity=-N)          鈫?搴撳瓨鐪熺浉婧?
    鈹?    └─[果] Inventory.quantity -= N, total_value -= N×avg_cost  鈫?缓存更新
     鈹?          └─[因果链] cost_engine.weighted_average() 重算加权平均
    鈹?
    ├─[因] FinanceEngine.record_sale(order)
    鈹?    └─[因] post_journal(account_id, "sale_order", source)
    鈹?          └─[因] JournalEngine.post(ledger_id, "sale_order", source)
    鈹?                ├─[因] _build_sale_order(source)
    鈹?                鈹?    └─[果] 鐢熸垚鍑瘉琛?
    鈹?                鈹?          鍊? 1122(应收账款) = total_with_tax
    鈹?                鈹?          璐? 6001(主营业务收入) = total_without_tax
    鈹?                鈹?          璐? 222101/222103(閿€椤圭◣棰? = tax_amount
    鈹?                鈹?          鍊? 6401(主营业务成本) = total_cost     [若COGS>0]
    鈹?                鈹?          璐? 1405(库存商品) = total_cost          [若COGS>0]
    鈹?                鈹?
    鈹?                ├─[因] _validate(lines) 鈫?借贷平衡校验
    鈹?                鈹?
    鈹?                ├─[果] AccountMove 创建 (move_type="sale_order")
    鈹?                鈹?
    鈹?                ├─[果] AccountMoveLine × N 创建
    鈹?                鈹?    └─[因果链] 每行触发:
    鈹?                鈹?          LedgerEngine.update_balance(line)
    鈹?                鈹?            └─[果] LedgerAccountBalance.debit_total += line.debit
    鈹?                鈹?            └─[果] LedgerAccountBalance.credit_total += line.credit
    鈹?                鈹?            └─[果] LedgerAccountBalance.balance += (debit - credit)
    鈹?                鈹?                  鈫?璐㈠姟浣欓鐪熺浉婧?
    鈹?                鈹?
    鈹?                └─[果] generate_voucher_number() 鈫?鍑瘉鍙?
    鈹?
    └─[因] bus.emit("sale_order.created")
          └─[果] AuditLog 写入
```

**鏁版嵁鐪熺浉婧愪紶鎾?**
- SaleOrder 鈫?StockMove 鈫?Inventory(缓存)
- SaleOrder 鈫?AccountMove 鈫?AccountMoveLine 鈫?LedgerAccountBalance(鐪熺浉婧?

---

## 2. 閲囪喘鍒涘缓鍥犳灉閾?

```
用户操作: 鍒涘缓閲囪喘鍗?
    鈹?
    鈻?
CreatePurchaseOrderHandler.handle()
    鈹?
    ├─[因] 创建 PurchaseOrder + PurchaseItem
    鈹?
    ├─[因] InventoryEngine.inbound(account_id, product_id, qty, unit_price, tax_rate, ...)
    鈹?    ├─[果] StockMove(type=in, quantity=+N)           鈫?搴撳瓨鐪熺浉婧?
    鈹?    ├─[果] Inventory.quantity += N, total_value += total_cost
     鈹?    鈹?    └─[因果链] cost_engine.weighted_average() 重算加权平均
    鈹?    └─[果] 返回 calculated_data {total_cost, tax_amount, total_amount}
    鈹?          └─[因果链] 注：calculated_data 不再传给 FinanceEngine（C2 重构后 FinanceEngine 自行从 order.items 计算税额）
    鈹?
    ├─[因] FinanceEngine.record_purchase(order)
    鈹?    └─[因] post_journal(account_id, "purchase_order", source)
    鈹?          └─[因] JournalEngine.post(ledger_id, "purchase_order", source)
    鈹?                ├─[因] _build_purchase_order(source)
    鈹?                鈹?    └─[果] 鐢熸垚鍑瘉琛?
    鈹?                鈹?          鍊? 1405(库存商品) = inventory_cost
    鈹?                鈹?          鍊? 222102(进项税额) = tax_amount   [涓€鑸撼绋庝汉]
    鈹?                鈹?          璐? 2202(应付账款) = total_with_tax
    鈹?                鈹?
    鈹?                ├─[果] AccountMove + AccountMoveLine × N
    鈹?                └─[果] LedgerAccountBalance 鏇存柊锛堝悓閿€鍞摼锛?
    鈹?
    └─[因] bus.emit("purchase_order.created")
          └─[果] AuditLog 写入
```

**关键因果（C2 重构后）:** FinanceEngine.record_purchase() 自行从 order.items 计算税额（split/combine + 纳税人税率），不再依赖 InventoryEngine 的 calculated_data。两引擎各自计算但结果一致（同一套价税分离逻辑）。

---

## 3. 閿€鍞彇娑堝洜鏋滈摼

```
用户操作: 鍙栨秷閿€鍞崟
    鈹?
    鈻?
CancelSaleOrderHandler.handle()
    鈹?
    ├─[因] SaleOrderDomain.transition_to("cancelled")
    鈹?    └─[果] SaleOrder.status = CANCELLED
    鈹?
    ├─[因] FinanceEngine.reverse_sale(order_id)
    鈹?    └─[因] reverse_journal(db, account_id, "sale_order", order_id)
    鈹?          ├─[果] 鏌ユ壘鍘?AccountMove (is_reversal=False)
    鈹?          ├─[果] 创建冲红 AccountMove (is_reversal=True, date=鍘熷嚟璇佹棩鏈?
    鈹?          ├─[果] 逐行创建 AccountMoveLine (借贷互换)
    鈹?          鈹?    └─[因果链] 每行触发:
    鈹?          鈹?          LedgerEngine.update_balance(鍐茬孩琛?
    鈹?          鈹?            └─[果] LedgerAccountBalance 反向更新
    鈹?          └─[幂等] 宸插瓨鍦ㄥ啿绾㈠嚟璇?鈫?返回 None
    鈹?
    ├─[因] InventoryEngine.reverse(account_id, product_id, qty, ...)
    鈹?    ├─[果] 鍐欏弽鍚?StockMove (source_type="sale_order_reversal")
    鈹?    └─[果] Inventory 缓存反向更新 (qty回补, value回补)
    鈹?
    └─[因] reverse_receipts(sale_order_id, db)
          ├─[果] 创建反向 Receipt (璐熼噾棰?
          ├─[果] 创建反向 BankTransaction
          ├─[果] BankEngine.update_balance() 鈫?BankAccount.balance 鍥為€€
          └─[果] SaleOrder.payment_status 更新
```

**鍥犳灉閾句氦鍙夐獙璇?** 鍐茬孩鍑瘉鏃ユ湡蹇呴』涓庡師鍑瘉鏃ユ湡涓€鑷达紙BR-22），否则 BS 报表 cutoff 杩囨护鏃朵細瀵艰嚧璧勪骇涓庢潈鐩婇敊閰嶃€?

---

## 4. 收付款因果链

### 4.1 收款

```
用户操作: 创建收款
    鈹?
    鈻?
create_receipt(db, receipt_in)
    鈹?
    ├─[因] 创建 Receipt 记录
    鈹?    └─[果] Receipt.amount = N
    鈹?
    ├─[因] 创建 BankTransaction (type="inflow")
    鈹?    └─[果] BankTransaction.amount = N
    鈹?
    ├─[因] BankEngine.update_balance(bank_account_id, amount, "inflow")
    鈹?    └─[果] BankAccount.balance += amount              鈫?银行余额缓存
    鈹?
    ├─[因] post_journal(account_id, "receipt", source)
    鈹?    └─[因] JournalEngine._build_receipt()
    鈹?          └─[果] 鍑瘉琛?
    鈹?                鍊? 1002(银行存款) / 1001(库存现金) = amount
    鈹?                璐? 1122(应收账款) = amount
    鈹?                └─[因果链] LedgerAccountBalance 更新
    鈹?
    └─[因] 更新 SaleOrder.payment_status
          └─[果] PAID / PARTIAL
```

### 4.2 付款

```
用户操作: 创建付款
    鈹?
    鈻?
create_payment(db, payment_in)
    鈹?
    ├─[因] 创建 Payment 记录
    ├─[因] 创建 BankTransaction (type="outflow")
    ├─[因] BankEngine.update_balance(bank_account_id, amount, "outflow")
    鈹?    └─[果] BankAccount.balance -= amount
    ├─[因] post_journal(account_id, "payment", source)
    鈹?    └─[因] JournalEngine._build_payment()
    鈹?          └─[果] 鍑瘉琛?
    鈹?                鍊? 2202(应付账款) = amount
    鈹?                璐? 1002(银行存款) / 1001(库存现金) = amount
    └─[因] 更新 PurchaseOrder.payment_status
```

**鍙屽啓涓€鑷存€?** 收付款同时写 BankTransaction(影响BankAccount) + AccountMoveLine(影响LedgerAccountBalance)锛?002科目余额与BankAccount浣欓蹇呴』涓€鑷淬€?

---

## 5. 鍙戠エ鍥犳灉閾?

### 5.1 发票创建联动

```
用户操作: 创建发票
    鈹?
    鈻?
CreateInvoiceHandler.handle()
    鈹?
    ├─[因] AccountingEngine.calculate_invoice_amounts(data)
    鈹?    └─[果] 价税分离: 涓嶅惈绋庨噾棰?= 含税金额 / (1+税率)
    鈹?
    ├─[因] validate_link_target() 鈫?校验关联目标
    ├─[因] 创建 Invoice + InvoiceItem
    鈹?
    ├─ 若关联销售单 鈫?_auto_generate_sale_order()
    鈹?  ├─[因] InventoryEngine.outbound()
    鈹?  鈹?    └─[果] StockMove + Inventory 更新
    鈹?  └─[因] FinanceEngine.record_sale()
    鈹?        └─[果] 閿€鍞嚟璇?+ LedgerAccountBalance 更新
    鈹?
    └─ 若关联采购单 鈫?_auto_generate_purchase_order()
      ├─[因] InventoryEngine.inbound()
      鈹?    └─[果] StockMove + Inventory 更新
      └─[因] FinanceEngine.record_purchase()
            └─[果] 采购凭证 + LedgerAccountBalance 更新
```

### 5.2 发票冲红级联

```
用户操作: 冲红发票
    鈹?
    鈻?
ReverseInvoiceHandler.handle()
    鈹?
    ├─[因] 鏍囪鍘熷彂绁?is_reversed = True
    ├─[因] 创建红字发票 (璐熼噾棰?
    鈹?
    ├─ 绾ц仈鍐茬孩锛堟鏌ラ儴鍒嗛€€璐ф暟閲忥紝鎸夊墿浣欓噺鍐茬孩锛?
    鈹?  ├─ 鍏宠仈閿€鍞崟 鈫?FinanceEngine.reverse_sale() + InventoryEngine.reverse()
    鈹?  ├─ 鍏宠仈閲囪喘鍗?鈫?FinanceEngine.reverse_purchase() + InventoryEngine.reverse()
    鈹?  └─[因果链] 凭证冲红 鈫?LedgerAccountBalance 反向更新
    鈹?             库存冲红 鈫?Inventory 反向更新
    鈹?
    └─[因] reverse_receipts() / reverse_payments()
          ├─[果] 反向 Receipt/Payment
          ├─[果] 反向 BankTransaction + BankAccount.balance 鍥為€€
          └─[果] 凭证冲红 鈫?LedgerAccountBalance 反向更新
```

**级联深度:** 发票冲红 鈫?涓氬姟鍗曞啿绾?鈫?鏀朵粯娆惧啿绾?鈫?閾惰浣欓鍥為€€ + 鍑瘉鍐茬孩銆傛渶澶?灞傚洜鏋滀紶鎾€?

---

## 6. 璐圭敤鍥犳灉閾?

```
用户操作: 创建费用
    鈹?
    鈻?
create_expense(db, expense_in)
    鈹?
    ├─[因] EXPENSE_ACCOUNT_CODE_MAP[category] 鈫?费用科目映射
    鈹?    └─[果] "閿€鍞垂鐢?鈫?602, "管理费用"鈫?601, "财务费用"鈫?603
    鈹?
    ├─[因] post_journal(account_id, "expense", source)
    鈹?    └─[因] JournalEngine._build_expense()
    鈹?          └─[果] 鍑瘉琛?
    鈹?                鍊? expense_account_code(6601/6602/6603) = amount
    鈹?                璐? 1002(银行存款) / 2202(应付账款) = amount
    鈹?                └─[因果链] LedgerAccountBalance 更新
    鈹?
    └─[因] crud._log() 鈫?AuditLog

费用冲红:
    鈹?
    鈻?
reverse_expense(db, expense_id)
    ├─[因] reverse_journal(db, account_id, "expense", expense_id)
    鈹?    └─[果] 冲红凭证 鈫?LedgerAccountBalance 反向更新
    └─[果] Expense.is_reversed = True
```

---

## 7. 鍥哄畾璧勪骇鍥犳灉閾?

### 7.1 折旧

```
月结触发 / 手动触发
    鈹?
    鈻?
FixedAssetEngine.record_depreciation(asset_id, period)
    鈹?
    ├─[因] calculate_monthly(asset) 鈫?骞撮檺骞冲潎娉?
    鈹?    └─[果] monthly = (鍘熷€济?1-娈嬪€肩巼)) / 使用月数
    鈹?
    ├─[果] FixedAssetDepreciation 流水创建         鈫?鎶樻棫鐪熺浉婧?
    ├─[果] FixedAsset.accumulated_depreciation += monthly  鈫?缓存更新
    鈹?
    └─[因] post_journal(account_id, "depreciation", source)
          └─[因] JournalEngine._build_depreciation()
                └─[果] 鍑瘉琛?
                      鍊? 6601(管理费用) = monthly
                      璐? 1602(累计折旧) = monthly
                      └─[因果链] LedgerAccountBalance 更新
```

**因果依赖:** 折旧影响利润 鈫?鍒╂鼎褰卞搷鎵€寰楃◣璁℃彁銆傛湀缁撴祦绋嬩腑鎶樻棫蹇呴』鍦ㄧ◣鍔¤鎻愪箣鍓嶆墽琛屻€?

### 7.2 资产处置

```
FixedAssetEngine.record_disposal(asset_id, disposal_price, disposal_date, bank_account_id)
    鈹?
    ├─[果] FixedAsset.status = "报废"
    鈹?
    ├─[因] 计算处置损益 = disposal_price - (鍘熷€?- 累计折旧)
    鈹?
    └─[因] post_journal(account_id, "asset_disposal", source)
          └─[因] JournalEngine._build_asset_disposal()
                └─[果] 鍑瘉琛?
                      鍊? 1602(累计折旧) = accumulated
                      璐? 1601(固定资产) = original
                      鍊? 1002(银行存款) = disposal_price    [若有收款]
                      璐? 6111(资产处置收益) = diff          [若赚]
                      鍊? 6711(资产处置损失) = |diff|        [若亏]
                      └─[因果链] LedgerAccountBalance 更新

    ├─[因] 鑻?disposal_price > 0 且有 bank_account_id:
    鈹?    ├─[果] BankTransaction 创建 (inflow, flow_category="investing")
    鈹?    └─[果] BankAccount.balance += disposal_price
```

---

## 8. 鏈堢粨鍥犳灉閾?

```
用户操作: 月末结账
    鈹?
    鈻?
MonthEndCloseHandler.handle(cmd, db)
    鈹?
    ├─[前置] 遍历 BankAccount 鈫?妫€鏌?BankReconciliation.status == "confirmed"
    鈹?        └─[阻断] 有未确认对账 鈫?鎶?BusinessError
    鈹?
    ├─[鍥?] FixedAssetEngine.batch_depreciate(period)
    鈹?    └─[果] 瀵规瘡涓湪鐢ㄨ祫浜?鈫?record_depreciation()
    鈹?          └─[因果链] 折旧凭证 鈫?LedgerAccountBalance 更新
    鈹?                └─[因果链] 影响利润总额 鈫?褰卞搷鎵€寰楃◣
    鈹?
    ├─[鍥?] TaxAccrualEngine.execute(account_id, period, taxpayer_type)
    鈹?    鈹?
    鈹?    ├─[因] 璁＄畻澧炲€肩◣:
    鈹?    鈹?    ├─ 涓€鑸撼绋庝汉: 222101(閿€椤? - 222102(进项) 鈫?杞嚭鏈氦澧炲€肩◣
    鈹?    鈹?    鈹?  └─[果] post_journal("vat_transfer_out")
    鈹?    鈹?    鈹?        鍊? 222106 鈫?璐? 222107
    鈹?    鈹?    └─ 小规模纳税人: 222103(閿€椤?
    鈹?    鈹?        └─[果] 鑻ユ湀閿€鍞墹10涓? post_journal("vat_exemption")
    鈹?    鈹?              鍊? 222103 鈫?璐? 6301(钀ヤ笟澶栨敹鍏?税收减免)
    鈹?    鈹?
    鈹?    ├─[因] 璁＄畻闄勫姞绋?
    鈹?    鈹?    └─ 城建7% + 教育附加3% + 地方教育2% × 瀹炵即澧炲€肩◣
    鈹?    鈹?    └─[果] post_journal("tax_surcharge")
    鈹?    鈹?          鍊? 6403(绋庨噾鍙婇檮鍔? 鈫?璐? 222104(搴斾氦闄勫姞绋?
    鈹?    鈹?
    鈹?    └─[因] 璁＄畻鎵€寰楃◣:
    鈹?          ├─ 应纳税所得额 = 利润总额(浠庣鐩綑棰濊鍙?
    鈹?          ├─ 小微: ×5% / 涓€鑸? ×25%
    鈹?          └─[果] post_journal("tax_income")
    鈹?                鍊? 6801(鎵€寰楃◣璐圭敤) 鈫?璐? 222105(搴斾氦鎵€寰楃◣)
    鈹?
    └─[鍥?] TaxCheckEngine.execute(period)
          ├─ 核对1: 222101余额 vs 6001×税率
          ├─ 核对2: 222102余额 vs 认证发票税额
          ├─ 核对3: 222104余额 vs 瀹炵即澧炲€肩◣脳璐圭巼
          ├─ 核对4: 222105余额 vs 利润×税率
          ├─ 核对5: 进项转出 vs 闈炲簲绋庨」鐩?
          └─ 核对6: 鍏嶇◣閿€鍞 vs 免税政策
```

**鍥犳灉搴?** 折旧(鍥?) 鈫?澧炲€肩◣(鍥?a) 鈫?闄勫姞绋?鍥?b,渚濊禆澧炲€肩◣) 鈫?鎵€寰楃◣(鍥?c,渚濊禆鍒╂鼎鍚姌鏃? 鈫?税务核对(鍥?,验证全部)

---

## 9. 閾惰瀵硅处鍥犳灉閾?

```
ImportBankStatement 鈫?BankStatement + BankStatementLine
    鈹?
    鈻?
ReconcileBank 鈫?BankReconcileEngine.auto_match()
    鈹?  ├─[因] 鎸夐噾棰?日期模糊匹配 BankStatementLine 鈫?BankTransaction
    鈹?  └─[果] 创建 BankReconciliation + ReconciliationItem
    鈹?
    鈻?
ForceMatch 鈫?BankReconcileEngine.force_match()
    鈹?  └─[果] 手动创建匹配关系
    鈹?
    鈻?
ConfirmBankReconciliation 鈫?BankReconcileEngine.confirm()
    鈹?  ├─[果] BankStatementLine.matched = True
    鈹?  └─[果] BankReconciliation.status = "confirmed"
    鈹?        └─[因果链] 姝ゆ椂鏈堢粨鍓嶇疆妫€鏌ユ墠鑳介€氳繃
    鈹?
    鈻?
GenerateReconciliationEntry 鈫?BankReconcileEngine.generate_entry()
    └─[因] post_journal("bank_fee_entry", source)
          └─[果] 閾惰鎵嬬画璐?利息凭证
                鍊? 6603(财务费用) / 璐? 1002(银行存款)  [手续费]
                鍊? 1002(银行存款) / 璐? 6603(财务费用)  [利息收入]
```

---

## 10. 鏈熷垵浣欓鍥犳灉閾?

```
CreateOpeningBalanceHandler.handle()
    鈹?
    ├─[前置] 鏍￠獙浼氳鎭掔瓑寮? 资产 = 璐熷€?+ 权益
    鈹?
    ├─[因] post_journal("opening_balance", source)
    鈹?    └─[因] JournalEngine._build_opening_balance()
    鈹?          └─[果] 鍔ㄦ€佺鐩锛屾寜浼犲叆 lines 生成
    鈹?                └─[因果链] LedgerAccountBalance 鍒濆鍖?
    鈹?
    └─[因] 同步 BankAccount 余额
          └─[果] BankAccount.balance = 期初银行存款余额

UpdateOpeningBalanceHandler.handle()
    鈹?
    ├─[因] reverse_journal(鍘熷嚟璇? 鈫?鍐茬孩鍘熸湡鍒濆嚟璇?
    └─[因] post_journal("opening_balance") 鈫?重新生成
```

---

## 11. 鏁版嵁鐪熺浉婧愪笌鍥犳灉渚濊禆姹囨€?

### 11.1 真相源关系图

```
StockMove (库存真相源 L2)
    鈹?
    鈹溾攢鈫?CostEngine.weighted_average (L2 内部计算真相源)
    鈹?    鈹斺攢鈫?Inventory (L4 缓存: quantity, average_cost, total_value)
    鈹?    鈹斺攢鈫?COGS计算 → 销售凭证的6401/1405行金额
    鈹?
    鈹斺攢鈫?AccountMoveLine.ledger_account_id=1405 的余额

AccountMove + AccountMoveLine (鍑瘉鐪熺浉婧?
    鈹?
    鈹斺攢鈫?LedgerAccountBalance (浣欓鐪熺浉婧?
          鈹溾攢鈫?璧勪骇璐熷€鸿〃 (balance_sheet)
          鈹溾攢鈫?鍒╂鼎琛?(income_statement)
          鈹溾攢鈫?税务计算 (TaxAccrualEngine 浠庣鐩綑棰濊鏁?
          鈹斺攢鈫?税务核对 (TaxCheckEngine 浠庣鐩綑棰濋獙璇?

FixedAssetDepreciation (鎶樻棫鐪熺浉婧?
    鈹?
    鈹斺攢鈫?FixedAsset.accumulated_depreciation (缓存)
          鈹斺攢鈫?处置损益计算 = disposal_price - (original - accumulated)

BankTransaction (閾惰娴佹按鐪熺浉婧?
    鈹?
    鈹斺攢鈫?BankAccount.balance (缓存)
          鈹斺攢鈫?涓?LedgerAccountBalance(科目1002) 蹇呴』涓€鑷?
```

### 11.2 因果依赖矩阵

| 鍥?| 鏋?| 传播路径 |
|----|-----|---------|
| SaleOrder 创建 | StockMove(out) | InventoryEngine.outbound() |
| SaleOrder 创建 | AccountMove(sale_order) | FinanceEngine.record_sale() 鈫?post_journal() |
| StockMove | Inventory 缓存 | 直接更新 |
| AccountMoveLine | LedgerAccountBalance | LedgerEngine.update_balance() |
| 折旧凭证 | 利润减少 | 6601借方增加 鈫?利润减少 |
| 利润减少 | 鎵€寰楃◣鍑忓皯 | TaxAccrualEngine 从科目余额读利润 |
| BankTransaction | BankAccount.balance | BankEngine.update_balance() |
| 鏀朵粯娆?| BankTransaction + AccountMove | 双写: 银行+财务 |
| 发票冲红 | 涓氬姟鍗曞啿绾?鈫?鏀朵粯娆惧啿绾?| 级联3-4灞?|
| 月结确认对账 | 月结前置通过 | BankReconciliation.status="confirmed" |

### 11.3 冲红幂等保障

| 操作 | 幂等机制 |
|------|---------|
| reverse_journal | 妫€鏌?is_reversal=True 的冲红凭证是否已存在 |
| InventoryEngine.reverse | 妫€鏌?source_type="{type}_reversal" 鐨?StockMove 鏄惁宸插瓨鍦?|
| post_journal | 妫€鏌?source_model+source_id 鐨勯潪鍐茬孩鍑瘉鏄惁宸插瓨鍦?|
| FixedAssetEngine.record_depreciation | 妫€鏌ュ悓 asset_id+period 的折旧流水是否已存在 |
| InventoryEngine.inbound/outbound | 妫€鏌ュ悓 source_type+source_id+product_id 鐨?StockMove 鏄惁宸插瓨鍦?|

---

## 12. 跨模块因果传播全景图

```
                    鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
                    鈹? 閿€鍞崟鍒涘缓  鈹?
                    鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹攢鈹€鈹€鈹€鈹€鈹€鈹?
                           鈹?
              鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹尖攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
              鈻?           鈻?           鈻?
        鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
        鈹?StockMove鈹?│AccountMove鈹?鈹? Receipt  鈹?
        鈹?(out)    鈹?鈹?sale_order)鈹?鈹?(鏀舵鏃?  鈹?
        鈹斺攢鈹€鈹€鈹€鈹攢鈹€鈹€鈹€鈹€鈹?鈹斺攢鈹€鈹€鈹€鈹攢鈹€鈹€鈹€鈹€鈹?鈹斺攢鈹€鈹€鈹€鈹攢鈹€鈹€鈹€鈹€鈹?
             鈹?           鈹?           鈹?
             鈻?           鈻?           鈻?
      鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
      鈹?Inventory  鈹?│LedgerAccount 鈹?│BankTransaction鈹?
      鈹?(缓存)     鈹?│Balance(真相) 鈹?鈹?             鈹?
      鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹攢鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹攢鈹€鈹€鈹€鈹€鈹€鈹€鈹?
                           鈹?               鈹?
                           鈻?               鈻?
                    鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?   鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
                    鈹? 财务报表   鈹?   │BankAccount 鈹?
                    鈹?BS/PL/绋?  鈹?   鈹?(缓存)     鈹?
                    鈹斺攢鈹€鈹€鈹€鈹€鈹攢鈹€鈹€鈹€鈹€鈹€鈹?   鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
                          鈹?
              鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹尖攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
              鈻?          鈻?          鈻?
        鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
        鈹傚鍊肩◣璁℃彁鈹?鈹傞檮鍔犵◣璁℃彁鈹?鈹傛墍寰楃◣璁℃彁鈹?
        鈹?渚濊禆閿€椤?鈹?鈹?渚濊禆瀹炵即鈹?鈹?渚濊禆鍒╂鼎鈹?
        鈹?进项余额)鈹?鈹?澧炲€肩◣) 鈹?鈹?总额)   鈹?
        鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
                          鈹?
                          鈻?
                   鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
                   鈹?税务核对  鈹?
                   鈹?6椤归獙璇? 鈹?
                   鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?
```
