---
doc-type: reference
---

# 错误信息规范

# 错误信息规范

## 核心原则

| # | 原则 | 说明 |
|---|------|------|
| 1 | **单一错误系统** | 鍏ㄧ郴缁熺粺涓€浣跨敤 `errors.py` 鐨?`BusinessError`（含 `ErrorCode` enum锛夛紝绂佹鑷缓閿欒绫?|
| 2 | **中文消息 + 妯℃澘鍖?* | 消息全中文，通过 `ERROR_REGISTRY` 模板 + `data` 参数生成，禁止硬编码拼接 |
| 3 | **璇箟鍖?ErrorCode** | 不同错误场景不同 code锛岀姝竴寰?`VALIDATION_ERROR` |
| 4 | **禁止 `message=str(e)`** | 姘歌繙涓嶈浼?raw Python exception 缁欑敤鎴?|
| 5 | **缁撴瀯鍖栧搷搴?* | 响应必须包含完整 6 字段：`code` / `message` / `action` / `action_data` / `data` / `ai_instruction` |

## 响应格式

```json
{
  "error": {
    "code": "PRODUCT_NOT_FOUND",
    "message": "鍟嗗搧涓嶅瓨鍦? ID=42",
    "action": "none",
    "action_data": {},
    "data": {"product_id": 42},
    "ai_instruction": "STOP_RETRYING. 商品不存在，请检查商品ID鎴栧悕绉版槸鍚︽纭?.."
  }
}
```

### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| `code` | string | 鉁?| ErrorCode enum 鍊硷紙澶у啓铔囧舰锛?|
| `message` | string | 鉁?| 涓枃娑堟伅锛岀粓绔敤鎴峰彲璇?|
| `action` | string | 鉁?| `none`/`retry`/`user_confirm`/`user_input`/`user_select`/`login`/`contact_admin` |
| `action_data` | object | 鉁?| action 鐨勯檮鍔犳暟鎹紙纭鎸夐挳鏂囨湰銆侀€夐」鍒楄〃绛夛級 |
| `data` | object | 鉁?| 与错误关联的业务数据（ID銆侀噾棰濄€佹暟閲忕瓑锛?|
| `ai_instruction` | string | 鉁?| 瀵?AI Agent 鐨勫紩瀵艰鏄?|

## ErrorCode 使用规范

### 鎸夐鍩熷垎缁?

| 分组 | code ǰ׺ | 示例 |
|------|-----------|------|
| 库存 | `INVENTORY_*` | `INVENTORY_INSUFFICIENT` |
| 订单 | `ORDER_*` | `ORDER_NOT_FOUND` |
| 发票 | `INVOICE_*` | `INVOICE_DUPLICATE_NUMBER` |
| 财务 | `BALANCE_*`, `CASH_FLOW_*`, `INCOME_STATEMENT_*` | `BALANCE_SHEET_UNBALANCED` |
| 商品 | `PRODUCT_*` | `PRODUCT_HAS_TRANSACTIONS` |
| 合作伙伴 | `SUPPLIER_*`, `CUSTOMER_*` | `CUSTOMER_HAS_ORDERS` |
| 鐜伴噾娴?| `CASH_FLOW_*` | `CASH_FLOW_NOT_FOUND` |
| 费用 | `EXPENSE_*` | `EXPENSE_NOT_FOUND` |
| 固定资产 | `FIXED_ASSET_*` | `FIXED_ASSET_NOT_FOUND` |
| 银行账户 | `BANK_ACCOUNT_*` | `BANK_ACCOUNT_NOT_FOUND` |
| 通用 | 鏃犲墠缂€ | `VALIDATION_ERROR`, `DUPLICATE_ENTRY` |

### 选择原则

```
找到了具体的领域实体 鈫?鐢ㄩ鍩?ErrorCode（PRODUCT_NOT_FOUND ✓）
浠呭仛鏍￠獙涓嶆秹鍙婂疄浣?鈫?鐢?VALIDATION_ERROR（带 details data锛?
数据重复 鈫?鐢?DUPLICATE_ENTRY（带 details data锛?
不应该发生的错误 鈫?鐢?INTERNAL_ERROR（同时带 context data锛?
```

## 鎶涘嚭閿欒鐨勬纭Э鍔?

### 鉁?正确：模板化（优先）

```python
raise BusinessError(
    code=ErrorCode.PRODUCT_NOT_FOUND,
    data={"product_id": product_id}
)
```

娑堟伅浠?`ERROR_REGISTRY` 模板 `"鍟嗗搧涓嶅瓨鍦? ID={product_id}"` 鑷姩鐢熸垚銆?

### 鉁?正确：模板化 + 覆盖消息

```python
raise BusinessError(
    code=ErrorCode.VALIDATION_ERROR,
    data={"details": "金额不能为负"},
)
```

### 鉂?错误：message=str(e)

```python
# 鍙嶆ā寮?- 禁止
raise BusinessError(code=ErrorCode.VALIDATION_ERROR, message=str(e))
```

### 鉂?错误：硬编码消息不用 data

```python
# 鍙嶆ā寮?- 禁止
raise BusinessError(code=ErrorCode.VALIDATION_ERROR, message="閾惰璐︽埛涓嶅瓨鍦?)
```

搴旇鏀逛负锛?

```python
raise BusinessError(
    code=ErrorCode.BANK_ACCOUNT_NOT_FOUND,
    data={"bank_account_id": bank_account_id}
)
```

### 鉂?閿欒锛氳嫳鏂囨秷鎭?

```python
# 鍙嶆ā寮?- 禁止
raise BusinessError(code=ErrorCode.VALIDATION_ERROR,
    message=f"category '{category}' not in {EXPENSE_CATEGORIES}")
```

## 会计错误处理

会计相关错误统一使用 `accounting_engine.AccountingError`（带 `accounting_rule` + `calculation_detail`），
娉ㄥ唽鍦?`main.py:85`銆?

禁止使用 `models_finance.AccountingError`锛堣鏃?handler 绫诲凡搴熷純锛夈€?

```python
# 鉁?正确
from accounting_engine import AccountingError, AccountingErrorCode

raise AccountingError(
    code=AccountingErrorCode.INVOICE_AMOUNTS_NOT_BALANCED,
    message=f"鍙戠エ閲戦涓嶅钩琛★細涓嶅惈绋?{a} + 税额 {b} 鈮?{c}锛堝樊棰?{diff}锛?,
    accounting_rule="《小企业会计准则》§二/2.1 发票金额计算",
    calculation_detail={...}
)
```

## 前端错误展示

前端必须通过 `handleError()` 缁熶竴澶勭悊锛岀姝㈢洿鎺ヨ鍙?`err.response?.data?.detail`锛?

```javascript
// 鉁?正确
catch (e) { handleError(e, { defaultMsg: '保存失败' }) }

// 鉂?鍙嶆ā寮?- 禁止
catch (e) { ElMessage.error(`失败: ${e.response?.data?.detail}`) }
```

## 反模式清单（Error-AP锛?

| # | 鍙嶆ā寮?| 后果 | 修复方式 |
|---|--------|------|----------|
| Error-AP1 | `message=str(e)` | 暴露 Python 瀹炵幇缁嗚妭缁欑敤鎴?| 改用 `data={}` 或语义化消息 |
| Error-AP2 | 纭紪鐮佹秷鎭笉鐢?data 模板 | 消息不一致，难以维护 | 璧?ERROR_REGISTRY 模板 |
| Error-AP3 | 不同语义错误共用同一 ErrorCode（特别是 VALIDATION_ERROR锛?| 鍓嶇鏃犳硶閽堝鎬у鐞?| 鍔犱笓鐢?ErrorCode |
| Error-AP4 | 英文/中英混杂消息 | 鐢ㄦ埛鐪嬩笉鎳?| 鍏ㄤ腑鏂囨秷鎭?|
| Error-AP5 | 自建错误类（AccountingError 閲嶅瀹氫箟锛?| 异常落入 generic 500 handler | 统一用已有错误类 |
| Error-AP6 | 前端直接 ElMessage.error 璇?detail | 涓㈠け缁撴瀯鍖栦俊鎭?| 鐢?handleError() |
| Error-AP7 | INTERNAL_ERROR 鐢ㄤ簬鍩熻鍒欎繚鎶?| 璇箟閿欒锛屽墠绔樉绀?鑱旂郴绠＄悊鍛? | 鐢?409/403 对应 code |

