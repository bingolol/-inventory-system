# 📦 功能模块说明

> 各功能模块的业务规则、数据模型、联动逻辑

**API 端点详见**: [AI_AGENT_GUIDE.md](./AI_AGENT_GUIDE.md)

---

## 📋 模块概览

| 模块 | 核心功能 |
|------|----------|
| 商品管理 | 商品 CRUD、分类管理、SKU 管理 |
| 库存管理 | 库存查询、盘点、预警、自动联动 |
| 供应商 | 供应商 CRUD、往来账统计 |
| 客户 | 客户 CRUD、往来账统计 |
| 采购管理 | 采购单 CRUD、入库联动、状态管理 |
| 销售管理 | 销售单 CRUD、出库联动、状态管理 |
| 发票管理 | 发票录入、认证、税额计算 |
| 费用管理 | 费用记录、分类统计 |
| 个人流水 | 个人收支记账 |
| 财务报表 | 资产负债表、利润表、现金流 |
| 固定资产 | 资产管理、折旧计算、发票关联 |
| **会计引擎** | **统一计算引擎（发票/折旧/增值税/所得税/报表校验）** |

---

## 📦 商品管理

### 功能说明

- 商品信息 CRUD
- 商品分类管理
- SKU 编码管理
- 库存预警设置

### 业务规则

1. **删除约束**: 存在采购或销售记录的商品不能删除
2. **SKU 唯一**: 同一账本下 SKU 不能重复
3. **库存跟踪**: `track_inventory` 字段控制是否跟踪库存

### 数据模型

```python
class Product:
    id: int
    account_id: int
    name: str
    sku: str
    category: str
    unit: str
    purchase_price: Decimal
    sale_price: Decimal
    min_stock: int
    track_inventory: bool
    description: str
```

---

## 📊 库存管理

### 功能说明

- 库存数量查询
- 库存预警（低于 min_stock）
- 库存盘点调整
- 库存变动日志

### 库存联动规则

| 操作 | 库存变化 |
|------|----------|
| 创建采购单 (track_inventory=true) | +quantity |
| 取消/删除采购单 (已完成) | -quantity |
| 创建销售单 (deduct_inventory=true, track_inventory=true) | -quantity |
| 取消/删除销售单 | +quantity |
| 库存盘点调整 | 设为指定值 |

### 业务规则

1. **库存不足校验**: 扣减时检查库存是否充足
2. **track_inventory**: False 的商品跳过库存操作
3. **原子操作**: 库存变动记录到 OperationLog

---

## 🤝 供应商管理

### 业务规则

1. **删除约束**: 存在采购记录的供应商不能删除
2. **名称唯一**: 同一账本下供应商名称不能重复

---

## 👥 客户管理

### 业务规则

1. **删除约束**: 存在销售记录的客户不能删除
2. **名称唯一**: 同一账本下客户名称不能重复

---

## 🛒 采购管理

### 功能说明

- 采购单 CRUD
- 采购入库联动
- 采购单状态管理
- 付款状态跟踪

### 业务规则

1. **入库联动**: 创建采购单自动增加库存
2. **回补联动**: 取消/删除已完成采购单自动减少库存
3. **状态机**: pending → completed → cancelled
4. **订单号格式**: CG{年}—{月}-{日}-{时}:{分}-{序号}

### 数据模型

```python
class PurchaseOrder:
    id: int
    account_id: int
    order_no: str
    supplier_id: int
    has_invoice: bool
    payment_method: str  # 'company' | 'private_advance'
    payment_status: str  # 'paid' | 'unpaid' | 'partial'
    status: str          # 'pending' | 'completed' | 'cancelled'
    total_price: Decimal
    notes: str
    items: List[PurchaseItem]
```

---

## 💰 销售管理

### 功能说明

- 销售单 CRUD
- 销售出库联动
- 销售单状态管理
- 收款状态跟踪

### 业务规则

1. **出库联动**: 创建销售单自动扣减库存
2. **回补联动**: 取消/删除已完成销售单自动增加库存
3. **库存校验**: 扣减前检查库存是否充足
4. **订单号格式**: XS{年}—{月}-{日}-{时}:{分}-{序号}

### 数据模型

```python
class SaleOrder:
    id: int
    account_id: int
    order_no: str
    customer_id: int
    deduct_inventory: bool
    payment_status: str
    status: str
    total_price: Decimal
    notes: str
    items: List[SaleItem]
```

---

## 🧾 发票管理

### 功能说明

- 进项/销项发票管理
- 普票/专票区分
- 专票认证状态
- 税额自动计算

### 业务规则

1. **发票号码唯一**: 同一账本下发票号码不能重复
2. **税额计算**: `tax_amount = amount_with_tax × tax_rate ÷ (1 + tax_rate)`
3. **认证状态**: n_a (不适用) → pending (待认证) → certified (已认证)

### 税率选项

| 税率 | 说明 |
|------|------|
| 0.01 | 1% (小规模优惠) |
| 0.03 | 3% |
| 0.06 | 6% (服务业) |
| 0.09 | 9% (交通运输) |
| 0.13 | 13% (一般商品) |

---

## 💸 费用管理

### 费用分类

| 分类 | 说明 |
|------|------|
| 房租 | 租金支出 |
| 水电 | 水电费 |
| 工资 | 员工工资 |
| 材料 | 材料采购 |
| 办公用品 | 办公耗材 |
| 运费 | 物流费用 |
| 维修 | 维修费用 |
| 其他 | 其他费用 |

---

## 👤 个人流水

### 分类

| 类型 | 分类选项 |
|------|----------|
| 收入 | 工资、兼职、理财、其他 |
| 支出 | 餐饮、日用、交通、娱乐、医疗、烟酒、其他 |

---

## 📊 财务报表

### 功能说明

- 资产负债表
- 利润表（经营口径）
- 财务汇总
- 现金流量表

---

## 🧮 税务报表

### 功能说明

- 增值税季度/月度报表
- 企业所得税报表
- 进项/销项发票汇总

---

## 🔧 固定资产

### 折旧方法

| 方法 | 说明 |
|------|------|
| 年限平均法 | 直线法，均匀折旧 |
| 双倍余额递减法 | 加速折旧 |
| 年数总和法 | 加速折旧 |

---

## 📝 操作日志

### 日志字段

| 字段 | 说明 |
|------|------|
| operation | create/update/delete/adjust |
| entity_type | product/purchase_order/sale_order/... |
| entity_id | 实体 ID |
| detail | 操作描述 |
| operator | 操作员 (user/ai) |

---

*功能模块说明 v2.1 | 2026-06-21*
